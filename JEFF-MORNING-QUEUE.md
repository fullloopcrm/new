# JEFF — MORNING QUEUE (gated decisions awaiting your approval)
> Overnight autonomous mode active from 2026-07-11 ~20:37. Leader keeps all NON-gated work flowing; every gated item lands here with full context. **Nothing below has been actioned — all await your explicit yes/no.**
> Format per item: TIME · DECISION NEEDED · CONTEXT (blocked/depends/workers) · RECOMMENDATION+why · BLAST RADIUS if wrong.

## 00:47 2026-07-18 · MEDIUM/HIGH, likely currently live in prod — unauthenticated cross-tenant Telegram/notification spoofing via /api/yinez (W4 finding, FIXED file-only)
- **Decision needed:** approve merge+deploy of the fix in the same batch as the other overnight security fixes — fix-review item, not a design question.
- **Context:** `notify()` (`lib/notify.ts` + `lib/nycmaid/notify.ts`) falls back to reading `x-tenant-id` from the request headers when a caller doesn't pass an explicit tenant, but — unlike every other tenant-header helper in the codebase (`getTenantFromHeaders`, `getCurrentTenant`, and `/api/yinez`'s own local check) — it never verified the middleware-signed `x-tenant-sig` companion. `POST /api/yinez` is fully public and, on the main host, middleware passes caller-set headers straight through unmodified for public routes. Result: anyone could `POST /api/yinez` with a hand-set `x-tenant-id: <any tenant>` header (no valid signature needed) and cause `notify()` to write a fabricated "New Web Chat Lead"/"Yinez Web Chat Error" row onto that tenant's own dashboard AND push a Telegram message — with attacker-influenced text — directly into that tenant's own configured Telegram bot/chat, impersonating a real lead alert. No account takeover and no data mutation (the route's actual DB writes were already correctly bound to a verified tenant), but a real unauthenticated cross-tenant spoofing/harassment vector into a live business's own alert channel.
- **Fix (file-only, p1-w4, commit `8a116826`):** both `notify()` fallbacks now verify `x-tenant-sig` via the same `verifyTenantHeaderSig()` used everywhere else, rejecting an unsigned/forged/mismatched header (falls back to "no tenant" / the global platform Telegram channel, same as no header at all). Zero behavior change for legitimate tenant-subdomain requests, which already carry a correctly-signed header. New regression tests lock the exact attack (forged header, garbage signature, signature valid for a *different* tenant than the id it's paired with) plus the legit-signed-header path, for both notify() variants — 15/15 pass, tsc clean.
- **Recommendation:** bundle into the same merge batch as the phone→email account-takeover fix above — same file-only/no-deploy state, same "real gap, already closed, just needs to reach main" shape.
- **Blast radius if left as-is:** ongoing until merged — any tenant's Telegram business channel can currently be spoofed by an unauthenticated caller who knows (or enumerates) their tenant UUID.

## 04:08 2026-07-18 · Product/rollout question — ComHub has been fully broken for real tenant owners, fix is well-understood but risks a live billing exposure if flipped blind (W2 finding, NOT fixed)
- **Decision needed:** should ComHub's 20 API routes (`/api/admin/comhub/**`) be switched from `requireAdmin()` (Jeff's global PIN only) to `getTenantForRequest()` (normal tenant auth), matching its 3 sibling dashboard sections (Loop, Messages, Connect) that already work this way for real owners? If yes, does it need a rollout gate given the cost exposure below?
- **Context:** ComHub's `/dashboard` nav entry has no permission field, identical to its 3 working siblings — but unlike them, every one of its 20 API routes is gated behind the global super-admin PIN instead of tenant auth. Practical effect: a real, non-impersonated tenant owner sees ComHub in their nav and gets a 401 on every single request. The feature has never actually worked for a real customer, only for someone impersonating via the global admin PIN.
- **Why not fixed blind (W2's call, correct):** the mechanical fix is simple and well-precedented (swap to the same pattern the 3 siblings already use) — but it touches cost-bearing send/SMS/email/voice-dial routes across all 20 files. Flipping this open unconditionally would give every existing tenant live, billable SMS/email/voice-dial capability overnight with no rollout gate or cost controls in front of it. Separately, a prior independent audit already looked at these same routes and treated `requireAdmin()` there as accepted-by-design — so this contradicts an earlier reviewed decision, not just an oversight, and needed a flag rather than a guess.
- **Recommendation:** none — genuine product call on whether/how to roll this out (staged per-tenant enablement vs. flip-all-at-once), separate from the underlying auth-pattern fix itself which is straightforward once the rollout question is answered.
- **Blast radius if left as-is:** none new — ComHub stays broken for real owners exactly as it has been. Blast radius risk is on the *fix* side (uncontrolled billing exposure), not on leaving it alone.

## 03:50 2026-07-18 · Product/legal question — e-sign signer PII has no client_id join path, so GDPR/CCPA erasure can't reach it (W2 finding, NOT fixed)
- **Decision needed:** should `document_signers` (e-signature signer records — name/email/phone captured at signing time) be included in the client-erasure purge, or is this data covered by a legal-retention carve-out (signed documents often need to be retained for legal/audit reasons independent of the signer's own erasure request)?
- **Context:** while fixing the erasure-purge gap that never reached `client_contacts` (see fix above), W2 found `document_signers` has the same denormalized-contact-snapshot shape but no `client_id` foreign key at all — there's no way to programmatically tie a signer record back to a client for automated purge, and it may be intentional (signed contracts/legal docs typically need retention regardless of erasure requests elsewhere).
- **Why not fixed blind (W2's call, correct):** this is a legal/compliance judgment call, not a technical gap — building automated purge without knowing whether retention is required would risk destroying records that need to be kept.
- **Recommendation:** none — genuine legal/product call. If purge is required, a client_id link needs to be added first; if retention is required, this should be explicitly documented as an exception.
- **Blast radius if left as-is:** none new — same behavior continues (signer PII survives an erasure request) until decided either way.

## 03:17 2026-07-18 · Test-infra gap, low urgency — shared fake-Supabase's `.or()` is a documented no-op, ~20 production routes' tests may not verify what they claim (W3 finding)
- **Decision needed:** none urgent — worth deciding whether this is worth a dedicated round to fix, given the blast radius of touching a shared test fixture other tests may rely on.
- **Context:** while fixing a real data-loss bug (see below), W3 found the shared `fake-supabase` test helper's `.or()` implementation is a documented no-op (its own comment: "don't rely on this to filter anything") — meaning any route whose real correctness depends on an `.or()` filter (~20 routes identified: `clients/search`, `admin/clients`, `comhub/search-recipients`, several `finance/*` routes, `team-portal/notifications`, etc.) has tests that pass regardless of whether the actual filter logic is correct. W3 deliberately did not touch the shared fixture itself — high blast radius, other tests may depend on the no-op behavior in ways that aren't obvious.
- **Recommendation:** none — a real call on how much test-fidelity risk to accept vs. the cost of fixing a widely-depended-on shared fixture.
- **Blast radius if left as-is:** low today (no known live bug found via this gap yet), but it means test coverage on ~20 routes is weaker than it looks — a future `.or()`-dependent bug on any of them could ship without a failing test catching it.

## 02:12 2026-07-18 · Operational near-miss, low severity, worth your awareness — a worker worktree has access to a real prod Supabase token (W3 finding)
- **Decision needed:** none urgent — worth deciding whether `p1-w3`'s (and likely the other worktrees') `~/.env.local` should be scrubbed of real prod credentials, or whether a test-only token should be provisioned instead.
- **Context:** W3's standing instruction is "TEST env only" for local `reconcile-tenant-config.mjs` runs. This round, the script picked up a REAL Supabase Management-API token from `~/.env.local` (not a test env) and issued live read-only SELECTs against prod (22 real tenants returned). The script is verified SELECT-only, so no writes occurred and nothing was harmed — but this means a worker worktree currently has filesystem access to a real prod credential it isn't supposed to use, a config-hygiene gap rather than a live incident.
- **Recommendation:** none — low urgency given no writes occurred, but worth deciding whether to scrub the credential from worker-accessible `.env.local` files or provision a real test-env token so this doesn't rely on the script's own read-only guarantee as the only safety net.
- **Blast radius if left as-is:** low today (script is read-only by design), but the underlying exposure (worker worktree can reach a live prod credential) is the kind of thing that becomes a real problem if a future script isn't as carefully scoped.

## 02:06 2026-07-18 · HIGH severity, structural gap — self-serve Stripe Checkout signups never get fully provisioned (W2 finding, NOT fixed, needs a call)
- **Decision needed:** should `webhooks/stripe/route.ts`'s full-loop-signup branch call `activateTenant()` (the same provisioning path its sibling `webhooks/stripe-platform/route.ts` already uses), or is this branch intentionally different? If yes, a real schema conflict needs resolving first — see below.
- **Context:** a tenant that signs up via self-serve Stripe Checkout gets `tenants.status` flipped to `active` unconditionally, with no team member created, no onboarding tasks, no chart of accounts/HR setup, no domain registration (`tenant_domains` + Vercel carrying domain), and no review destination configured. Its sibling webhook route does all of this via `activateTenant()`. This branch instead hand-rolls its own partial provisioning (which W2 also found was masking a DB error and duplicating an existing helper — that part is fixed).
- **Why not fixed blind (W2's call, correct):** `activateTenant()` does an immediate PIN-based owner-login insert, but this signup path uses a Clerk-based invite-accept flow (`accept-invite.ts`) instead — wiring the two together risks a real, currently-unverified conflict. W2 could not confirm the live schema because `schema.sql` on disk is stale (missing `pin_hash`/`pin_set_at` columns the code actually writes) and W2 has no DB access from its worktree.
- **Recommendation:** none — genuine product/architecture call, plus a schema-verification step (leader can run a read-only query to confirm live columns, same pattern used for other schema-ambiguity items tonight) before any fix is attempted.
- **Blast radius if left as-is:** ongoing — every self-serve Stripe Checkout signup is a tenant missing basic operational setup (no team access beyond the owner, no domain, no accounting scaffolding) until this is decided and fixed.

## 01:53 2026-07-18 · CRITICAL, likely currently live in prod — third entry point into the same owner-tool takeover class, this one hits NYC Maid specifically (W4 finding, FIXED file-only)
- **Decision needed:** approve merge+deploy priority, same class as the 01:41 chat/yinez fix and the other CRITICAL items above.
- **Context:** the per-tenant Telegram webhook (`/api/webhooks/telegram/[tenant]`) failed OPEN when `tenants.telegram_chat_id` was unset — a real, reachable state (a bot-token save auto-registers the live webhook independently of whether `chat_id` is set, e.g. mid-onboarding). Any stranger who found a tenant's bot could message it and drive `askSelena` using the platform owner phone as the caller identity — for NYC Maid specifically, that phone passes `isOwnerOfTenant()`'s `OWNER_PHONES` fallback, granting full owner-tool access (refunds, broadcasts, revenue, settings, cron) with zero proof of possession. This is the third entry point into the same class as the 01:41 `/api/chat`+`/api/yinez` fix.
- **Fix (file-only, p1-w4, commit `b50e954b`):** the route now fails closed on an unset `chat_id`, matching the 2 sibling Telegram routes that never had this gap. RED/GREEN test-verified, tsc clean, full suite green.
- **Worth noting:** W4 found this via a new systematic method — a repo-wide grep sweep for routes missing an auth-guard call (40 candidates surfaced, ~25 still unchecked) — rather than continuing down a named list. Recommends continuing that sweep next; likely the most efficient way to find any remaining entry points into this same class.
- **Blast radius if left as-is:** ongoing, and specifically dangerous for NYC Maid given the owner-phone fallback — anyone who finds a tenant's bot during onboarding (before chat_id is set) can drive real owner-level actions with zero authentication.

## 01:41 2026-07-18 · CRITICAL, likely currently live in prod — unauthenticated owner-tool takeover via public web-chat widgets (W4 finding, FIXED file-only)
- **Decision needed:** approve merge+deploy priority, same class as the Stripe webhook forgery and client-email-takeover items above (fix-review item, not a design question).
- **Context:** `/api/chat` and `/api/yinez` (both public, unauthenticated web-chat widgets) forwarded a caller-supplied `phone` field straight into `isOwnerOfTenant()`. Anyone who knows or guesses a tenant's `owner_phone` could claim to be that owner with zero authentication and unlock owner-level AI tool access: `process_stripe_refund`, `send_broadcast`, `get_revenue`, `update_setting`, `trigger_cron`, `block_client`. This is existing code, not new from tonight — likely live in prod as-is.
- **Fix (file-only, p1-w4, commit `2a684baf`):** both routes now strip a self-reported phone that would pass the owner check before it ever reaches the agent. 2 new + 4 updated tests, all green, tsc clean.
- **Two residual items flagged by W4 for your call, not acted on:** a narrow unverified-tenant edge case on `/api/yinez`, and a broader architectural question — whether client-facing chat (`askSelena`, ~11 call sites) needs a real verified/unverified-phone flag threaded through, same shape as the earlier Jefe prompt-injection finding.
- **Blast radius if left as-is:** ongoing — anyone who knows or can guess a tenant owner's phone number can currently impersonate them through the public chat widget and trigger real refunds, broadcasts, and settings changes, until this merges and deploys.

## 01:11 2026-07-18 · Ops action needed once merged — Stripe webhook endpoint isn't subscribed to `charge.dispute.closed` (W1 finding)
- **Decision needed:** none — just an ops step. Once the fix below merges+deploys, add `charge.dispute.closed` to the Stripe webhook endpoint's subscribed-events list (Stripe Dashboard or API), outside this repo.
- **Context:** W1 found `charge.dispute.closed` had no handler at all — a WON dispute (Stripe returns the funds) never reversed the original chargeback loss booked by `charge.dispute.created`, permanently understating loss on every won dispute. Fixed (`postChargebackReversalToLedger`, DR 1050/CR 6110, guarded against posting without a matching original chargeback entry), file-only, not deployed. But the fix does nothing unless Stripe is actually configured to deliver that event to this webhook.
- **Blast radius if left as-is:** the code fix is inert until the Stripe-side subscription is added — won disputes keep silently overstating loss forever, same as before.

## 01:11 2026-07-18 · Security gap, prepared not wired — team_members.pin and clients.pin stored in plaintext (W1 finding)
- **Decision needed:** approve scheduling the cutover to hash-compare auth (team-portal/auth, client/login, cleaners routes, client/collect, client/verify-code, client/book) — same treatment as the Telegram-webhook-secret gap: needs the backfill run in prod FIRST, then a coordinated code cutover, not a simultaneous flip.
- **Context:** `team_members.pin`/`clients.pin` are stored plaintext (unlike `tenant_members.pin_hash`, which is deliberately HMAC-SHA256-hashed). A DB-only leak (backup exfil, misconfigured read access) exposes every cleaner's and every client's live login PIN in the clear today. W1 prepared this as files only, correctly not touching live auth code yet: new `hashClientPin`/`hashTeamMemberPin` helpers (`src/lib/pin-hash.ts`), a nullable-first migration adding `pin_hash`/`pin_hash_set_at` to both tables, and an idempotent backfill script (secret passed via `psql -v`, never hardcoded, fail-loud verification built in). Deliberately left the client-PIN unique index non-unique — no collision check exists today on the 3 client-PIN write sites, so enforcing uniqueness now would risk the migration itself failing on a pre-existing duplicate.
- **Recommendation:** schedule in this order — (1) run the migration, (2) run the backfill in prod, (3) cut over the ~6 read/write auth call sites to hash-compare, verified working, before considering the plaintext columns removable. Do not flip auth code before the backfill has actually run, or every cleaner/client login breaks instantly.
- **Blast radius if left as-is:** ongoing exposure — any DB-level leak hands out every login PIN in the clear, platform-wide.

## 00:31 2026-07-18 · CRITICAL, likely currently live in prod — client-portal account takeover via 5 public lead/contact forms (W4 finding, FIXED file-only)
- **Decision needed:** approve merge+deploy of the fix once it's part of whatever batch reaches main (fix-review item, not a design question — straightforward, same shape as other fixes tonight).
- **Context:** `/api/lead`, `/api/contact`, `/api/portal/collect`, `/api/client/collect`, `/api/ingest/lead` — 5 unauthenticated/shared-secret public lead forms — all dedupe an inbound submission by phone, then unconditionally overwrite the matched client's `email` column. `clients.email` doubles as the portal login identifier (`verify-code` matches by phone then email). Anyone who merely knows a client's phone number could submit one of these public forms with an attacker-controlled email, silently redirect that client's portal login, and hijack their 30-day portal session. This is existing code, not new from tonight — likely live in prod as-is right now.
- **Fix (file-only, p1-w4, commits `786fc1e6`/`12159cd1`/`215c23a7`):** all 5 sites now only set `email` when the matched client has none on file yet (first-write-wins, no overwrite of an existing value). RED/GREEN mutation-verified per site, tsc clean, full suite green (2 pre-existing documented baseline failures only, zero regressions). Swept 6 related surfaces (verify-code, campaigns/send, webhooks/stripe, portal/auth, portal/messages, test/email-selena) for a 6th instance — none found.
- **Recommendation:** prioritize in whatever merge batch reaches main first, same urgency class as the Stripe webhook forgery and Telegram auth gap already in this queue — real account-takeover path, live today.
- **Blast radius if left as-is:** ongoing — anyone with a client's phone number can currently redirect that client's portal login email and take over their session, until this merges and deploys.

## 00:21 2026-07-18 · URGENT, ops action needed — 3 GitHub repo secrets were never configured, two safety nets have been silently no-op since inception (W3 finding)
- **Decision needed:** add `SUPABASE_ACCESS_TOKEN_FULLLOOP`, `SUPABASE_DB_URL`, and `BACKUP_ENCRYPTION_KEY` as real GitHub Actions repo secrets (Settings → Secrets and variables → Actions on `fullloopcrm/new`). This is an ops action, not a code review — no worker can do this from inside a worktree.
- **Context:** W3 ran `gh secret list` against the real repo (not source-code assumption) and confirmed only `TELEGRAM_BOT_TOKEN`/`TELEGRAM_CHAT_ID` exist. Two consequences, both real: (1) `reconcile-tenant-config.mjs`'s tenant-config drift-detection CI gate has a token-guard that skips cleanly when the token is absent — every merge since this gate's inception has silently hit that skip path, including through the 2026-07-10 outage this gate was built to catch. It has never actually run in production CI. (2) `SUPABASE_DB_URL`/`BACKUP_ENCRYPTION_KEY` are also absent, so the nightly full-DB backup workflow has presumably been failing since inception — and its own failure alert pointed at `TELEGRAM_NOTIFY_CHAT_ID`, a secret that was never configured either, silencing the failure twice over.
- **Fixed (file-only, W3, this session):** `db-backup.yml`'s alert now points at `TELEGRAM_CHAT_ID` (the secret that actually exists, same one `ci.yml`/reconcile already alert through) — so once the missing secrets are added, a future backup failure will actually notify. The missing secrets themselves cannot be added from a worker worktree.
- **Recommendation:** add all 3 secrets as soon as convenient — until then, config drift can silently reach prod undetected and backups may not exist at all if something goes wrong.
- **Blast radius if left as-is:** potentially severe and invisible — if the nightly backup has genuinely been failing since inception, there may be no real DB backup safety net right now. Worth confirming backup status directly once the secret is added, not just trusting the workflow goes green.

## 15:17 2026-07-17 · Compliance question — shared-platform-Telnyx-number fallback for tenants without their own 10DLC registration (W2 finding)
- **Decision needed:** should SMS sends fall back to the shared platform Telnyx number/key when a tenant hasn't configured their own (current behavior on `bookings/batch/route.ts`, the one outlier), or should every caller skip-if-unconfigured instead (current behavior on the other ~40 call sites)?
- **Context:** while fixing a real bug (telnyx_phone/telnyx_api_key missing a fallback to the older `sms_number` column — ~40 call sites affected, fixed at the shared-library layer: `notify.ts`, `payment-processor.ts`, `notify-team.ts`, `admin-contacts.ts`, `comms-prefs.ts`; ~35 direct API-route call sites still pending incremental follow-up), W2 found `bookings/batch/route.ts` is the one caller that falls back to a shared platform Telnyx number/key when a tenant has none. Every other caller just skips sending. The onboarding checklist tracks `telnyx_compliance_submitted`/`approved` **per tenant** (10DLC carrier registration) — texting a tenant's customers from the shared platform number without that tenant's own carrier registration on file is a real 10DLC/TCPA-adjacent compliance question, not a technical one.
- **Recommendation:** none — genuine compliance call. If skip-if-unconfigured is correct, `bookings/batch` needs to be brought in line with the other 40. If platform-fallback is correct, it needs to be applied consistently (and someone should confirm the shared platform number itself carries whatever registration covers multi-tenant use).
- **Blast radius if left as-is:** ongoing inconsistency — one route sends from an unregistered-for-that-tenant number today, the other ~40 silently skip. Needs your call before W2 touches the remaining ~35 direct-route call sites, since the fix direction depends on which behavior is correct.

## 16:45 2026-07-17 · Architecture question — Stripe webhook has one global signing secret, unclear if tenants with their own Stripe account can reliably use it (W1 finding, leader-verified scope)
- **Decision needed:** does `webhooks/stripe/route.ts` need a per-tenant webhook secret (schema change + Stripe dashboard config per tenant), or is the single global `STRIPE_WEBHOOK_SECRET` intentional because independent-Stripe-account tenants use a different payment-confirmation path entirely?
- **Context:** while fixing a related, narrower bug (team-member Stripe Connect onboarding used the platform's global key instead of `tenant.stripe_api_key`, causing Connect accounts to be created under the wrong Stripe account and silently failing real payout transfers — **fixed**, file-only, commit 07bef3eb), W1 found the webhook's own inline team-member payout transfer (line ~518) has the identical platform-only-key bug, but did NOT fix it because it's entangled with a bigger open question: the webhook's signature verification uses a single global `STRIPE_WEBHOOK_SECRET` with no per-tenant secret column anywhere in the schema. It's unclear whether a tenant with their own independent Stripe account can ever successfully deliver webhook events to this endpoint at all.
- **Scope, verified via a leader-run read-only prod query (zero writes):** only **1 of 40 live tenants** actually has a distinct `stripe_api_key` configured — `we-pay-you-junk`. This is a narrow-blast-radius issue affecting that one tenant, not a platform-wide problem.
- **Recommendation:** none — genuine architecture call. If `we-pay-you-junk` is expected to receive its own Stripe webhook events, this needs real per-tenant secret plumbing. If it's not (e.g. that tenant's payments actually settle through the platform account regardless of its own key), the finding is moot and can be closed as a non-issue.
- **Blast radius if left as-is:** low (1 tenant), but that tenant's Connect-account payouts may currently be silently failing exactly like the now-fixed onboarding bug was, until this is resolved.
- **Update 17:38 2026-07-17 (W1):** the payout-transfer half of this is now fixed — the webhook's `transfers.create`/`payouts.create` call site now threads `tenant.stripe_api_key` through, same fix shape as the onboarding bug, leaving signature verification and the subscription-deleted lookup correctly on the platform key (those aren't tenant-specific). Grepped every `stripe.transfers.create`/`payouts.create`/`accounts.create`/`accountLinks.create`/`accounts.retrieve` call site app-wide to confirm this was the last outlier — the tenant-key-vs-platform-key bug class is now fully closed. **The webhook-signature architecture question above is unchanged and still open** — this update only closes the money-movement half, not the "can we-pay-you-junk's own Stripe account even reach this endpoint" question.

## 14:52 2026-07-17 · Architecture question — campaign send analytics have been broken for 3 months, two divergent routes need reconciling (W1 finding)
- **Decision needed:** which campaign-send route becomes canonical — `campaigns/[id]/send` (the one the dashboard's "Send Now" button actually calls, has the approval-gate + audit call, but never writes `campaign_recipients` rows or calls `notify()`) or `campaigns/send` (the one with full tracking — `notify()` + recipient insert + retry-PUT, plus `client_ids`/`recipient_filter` support — but has zero live callers anywhere in the app)?
- **Context:** confirmed via repo-wide grep + git blame — `campaigns/[id]/send` was wired to the panel by commit `74021bfc` (2026-04-25), meaning the tracked/instrumented route has been orphaned for **3 months**. Practical effect: no matter what lands from the earlier `campaign_recipients` tracking-column work (migrations 070/071), the live send path never creates a row for `resend`/`telnyx` webhooks to match against — delivered/opened counts can never move off zero for a real campaign send. Also found: `[id]/page.tsx`'s stat tiles read `open_count`/`click_count`, neither of which exists as a column anywhere (real name is `opened_count`; `click_count` has no producer at all) — a second, compounding display bug on top of the routing gap.
- **Why not fixed blind (W1's call, correct):** the two routes have genuinely diverging business rules beyond just "one tracks, one doesn't" — approval-gate and audit logging exist only on the live route, `client_ids`/`recipient_filter` support exists only on the tracked route. Merging them is a real reconciliation decision, not a mechanical patch — same standard as the `availability.manage_others` RBAC gap flagged earlier this session.
- **Recommendation:** none — depends on which route's business rules should win. Once decided, the fix is straightforward (port the missing rules onto whichever route becomes canonical, delete the other, fix the two stat-tile column names).
- **Blast radius if left as-is:** none new — campaign delivery/open tracking continues silently reading zero, same as it has for 3 months, until this is decided.

## 14:06 2026-07-17 · CRITICAL, likely currently live in prod — cross-tenant payment forgery via Stripe webhook (W4 finding)
- **Decision needed:** approve merge+deploy of the fix once it's part of whatever batch reaches main (this is a fix-review item, not a design question — the fix shape is straightforward and matches existing patterns in the same file).
- **Context:** `webhooks/stripe`'s `checkout.session.completed` handler resolved a booking from `session.client_reference_id` with **zero tenant filter**. That field is a public, payer-controlled Payment Link parameter — a payer using NYC Maid's static Payment Link could set `client_reference_id` to mark ANY other tenant's booking paid/partial, triggering a real cross-tenant Stripe Connect payout. This is existing webhook code untouched all session until W4's fresh-surface sweep found it just now — not new code from tonight's work, so this gap has likely been live in prod the whole time, not something introduced this session.
- **Fix (file-only, p1-w4, commits 47253ed6/108813be):** scoped the lookup to `NYCMAID_TENANT_ID`, matching the same file's existing pattern (`email-recovery` fallback + `charge.refunded`'s `tenantFromPaymentIntent`) rather than inventing a new approach. Proven end-to-end: a new test reproduces the actual forged paid+payout (RED), passes after the fix (GREEN). tsc clean, 23 tests green across webhooks/stripe+payments. W4 also checked the sibling `prospectId` `client_reference_id` path and correctly left it flagged-not-fixed (non-enumerable UUID, no attacker-favorable exploit — genuinely lower severity, not the same gap).
- **Recommendation:** prioritize this fix in whatever merge batch reaches main first, given the live money exposure — same class of urgency as the earlier Telegram webhook auth gap and the stripe-webhook money-race, both of which you prioritized ahead of the general batch.
- **Blast radius if left as-is:** ongoing — any payer with access to any tenant's Stripe Payment Link can currently mark an arbitrary other tenant's booking paid and trigger a real Connect payout, until this merges and deploys.

## 12:23 2026-07-17 · Product question — neighborhood-based lead/booking attribution has been dead since inception (W2 finding)
- **Decision needed:** should `tenant_domains.neighborhood`/`zip_codes`/`type` columns be added via migration (reviving the feature), or should `attribution.ts`'s neighborhood-based attribution code be deleted as never-actually-built?
- **Context:** confirmed live (real 42703 column-not-exist error + full sweep of migrations 043/046/058) that these columns have never existed on `tenant_domains`. Every call site in `attribution.ts` catches the resulting error silently, so this never crashed — it just never attributed a single lead or booking by neighborhood since the feature was written. Separately, a real masked-error bug in the same code path (silently swallowing errors and returning `[]`) is already fixed.
- **Recommendation:** none — depends entirely on whether neighborhood-based attribution is still wanted as a feature. If yes, needs a real migration + backfill; if no, it's a straightforward deletion.
- **Blast radius if left as-is:** none new — same silent no-op behavior continues either way until decided.

## 11:48 2026-07-17 · Architecture question — Telegram-capable notify path only reaches 6 of 54 real callers (W3 finding)
- **Decision needed:** should Telegram support be added to `lib/notify.ts` (the global dispatcher, 48 real operational callers: bookings/payments/webhooks/crons/team-portal) so it can benefit all of them, or should the 4 currently-dead `TELEGRAM_NOTIFY_TYPES` entries (`running_late`, `cleaner_paid`, `tip_paid`, `payment_received`) be retired as aspirational-never-built?
- **Context:** `nycmaid/notify.ts` (the Telegram-capable dispatcher) has only 6 callers — Selena/Yinez plus login/error-logger. The 4 entries above already fire real, correct notifications today through other channels (SMS/email/in-app) — they just never reach Telegram, despite being declared in the Telegram-type union as if they should. No existing precedent anywhere in the codebase for a modern route dual-firing both notify systems.
- **Recommendation:** none — genuine product/architecture call, not a technical one. Building the bridge is real feature work (not a file-only patch); retiring the entries is a one-line cleanup. W3 correctly declined to guess.
- **Blast radius if left as-is:** none new — current behavior (no Telegram ping for these 4 event types) continues unchanged either way until decided.

## 09:10 2026-07-17 · Product/money question — recurring-conversion referrals earn the referrer $0, forever (W1 finding)
- **Decision needed:** should a referred client who converts to a RECURRING service (not one-off) generate referral commission on their visits — first visit only (one-time-acquisition model), or every visit for the life of the schedule (per-visit/lifetime model)?
- **Context:** every one-off/manual booking route (client/book, client/collect, portal/collect) resolves+sets `bookings.referrer_id` off a `ref_code` present on that specific request, and team-portal/checkout's commission logic reads ONLY `booking.referrer_id` with no client-level fallback. All 4 recurring-series creation paths (admin/recurring-schedules, client/recurring, sale-to-recurring.ts, cron/generate-recurring) never touch `referrer_id` at all — a referred client who signs up for ANY recurring service earns the referrer $0 on every visit, including the first.
- **Why not fixed blind (W1's call, correct):** genuinely ambiguous which model is intended. The existing one-off repeat-booking behavior also misses commission without a re-sent ref_code (weak evidence for one-time-acquisition), but `referral_commissions`' UNIQUE(booking_id) dedup key (not a per-client cap) suggests every booking is meant to be commission-eligible (weak evidence for per-visit). Real money on both sides of guessing wrong.
- **Recommendation:** none — genuine product call, not a technical one. Once decided, the fix is small (thread referrer_id through the 4 recurring-creation paths + generate-recurring's per-occurrence write, same pattern as the referrer_id resolution already used on the one-off paths).
- **Blast radius if left as-is:** ongoing — every recurring signup that came through a referral link silently pays the referrer nothing, indefinitely, until this is decided either way.

**Open gated items: 12 (STALE, dated 2026-07-13 23:23)** · **[RECONCILED 2026-07-15 ~18:xx by session-16 leader]** Real status of the biggest items below: Q3 (Part 0 release pipeline) is now FULLY SHIPPED — see its own update note. Team_members.pin, the 5 legacy referral pages, and the Telegram webhook auth gap are each individually marked RESOLVED inline already (search "RESOLVED" below). Q1/Q2 (DNS: fladumpsterrentals, toll-trucks-near-me) — not touched this session, still genuinely open, still Jeff/registrar-only. Q4 (engine cutover decision) — not decided, still open. Q5 (6 leftover test tenants) — not re-checked this session, status unknown. Everything from "session-8 tick" downward (17:39-23:43 timestamped items) has NOT been individually re-verified this pass — most read as already fixed file-only on some branch, but whether that branch is now part of the merged/deployed Part-0 code needs the same per-item check Section B of MASTER-TODO-LIST.md just got (7/~25 sampled, confirmed live). Treat this file's real open count as "Q1, Q2, Q4, Q5, plus whatever the un-sampled 17:39-23:43 items turn out to still need" — not 12.

## 10:05 2026-07-16 · Product/architecture call — Edge middleware domain cache has no invalidation path (W2 finding)
- **Decision needed:** tenant-lookup.ts's in-memory domain/slug cache (5-min TTL, Edge isolate) has no invalidate-on-write when POST /api/admin/websites reassigns a domain between tenants. During a handoff, stale-cached requests can keep serving the OLD tenant's site/dashboard under that domain for up to 5 min.
- **Recommendation (W2, not acted on):** either a DB version-stamp check per request (kills the caching win) or shorten the TTL — a real tradeoff, not a bug fix, so not changed unilaterally.
- **Blast radius if left as-is:** low-frequency (only fires during an actual domain reassignment), but a real live-serving-wrong-tenant window when it does.

## 23:21 · CRITICAL, currently live in prod — legacy NYC Maid admin auth has NO tenant binding at all (W4 finding)
- **Decision needed:** approve a fix approach — either add a `tenant_id` column + filter to `admin_users` and every call site, or accelerate cutting these remaining routes over to the Clerk-backed tenant auth that's already planned. Not a one-file patch, explicitly not something to fix unilaterally.
- **Context:** `isAdminAuthenticated()` (src/lib/nycmaid/auth.ts) grants admin access app-wide off a single global `ADMIN_PASSWORD`-signed cookie. The `admin_users` table is queried with zero `tenant_id` filter at every call site checked so far: auth/login, admin-contacts, and 4+ per-tenant site clones. This is a pre-Clerk-migration relic (`admin_users` was meant to become `tenant_members`, per a comment in migrations/2026_05_19_remaining_tables.sql) that never got the tenant-scoping the newer system has. Practical effect: any legacy-admin session, from any tenant, passes the isAdmin check on routes gated this way (client/properties confirmed; likely others) and can read/write another tenant's client data by supplying a foreign `client_id`. This is separate from (but related to) two smaller already-fixed items earlier tonight: W3's 18:38/18:41 fixes closed an *instant-revocation* gap on this same legacy admin flow (disabling an admin_users row now takes effect immediately instead of waiting for 24h token expiry) — but that fix did not and could not add tenant binding, since the underlying table has no tenant_id column at all.
- **Recommendation:** given a full Clerk cutover is already planned per prior sessions' notes, the fastest safe interim fix is probably a `tenant_id` column + filter added to `admin_users` and its ~handful of call sites (auth/login, admin-contacts, client/properties, the 4 site clones) — narrower blast radius than pushing the Clerk migration timeline. Either way this needs your call since it touches the live NYC Maid admin surface.
- **Blast radius if left as-is:** any legacy admin session on any tenant can read/write another tenant's client records (name/email/phone/address/history) via at least one confirmed route (client/properties) and likely more gated the same way — a real live cross-tenant data-access hole, not merely theoretical.
- **Update 23:57 (W1):** found and patched a second confirmed symptom of this same root cause — `GET /api/client/properties?include_history=true` was leaking `property_changes` rows cross-tenant via this same tenant-unbound legacy cookie (same class as the already-fixed Selena convoId leak). Fixed narrowly at that one route (now resolves the client's own tenant_id and requires every returned row match it, commit fc46188a) — this is a symptom patch, not a fix to the root cause. The underlying `admin_users` table still has no `tenant_id` column, so any other route gated by `isAdminAuthenticated()` that hasn't been individually checked/patched remains exposed. The decision above still stands.
- **Update 00:46 (W4):** found a third confirmed affected route — `admin/bookings/[id]/closeout-summary`'s own bookings query has zero `tenant_id` filter, same `isAdminAuthenticated()` root cause. Correctly left unfixed as an isolated patch (would just be another symptom-only Band-Aid); flagging as further evidence the exposure is broad across the admin surface, not a one-off. Strengthens the case for the root-cause fix over continuing to chase individual routes.

## 21:36 · 5 legacy referral pages will break after W1's PII-leak fix, one linked from NYC Maid's live nav (W1 finding)
- **Decision needed:** approve repointing 5 legacy `site/<tenant>/referral` clone pages (template, the-florida-maid, wash-and-fold-hoboken, wash-and-fold-nyc, nycmaid) to the existing secure top-level `/referral` + `/referral/[code]` email-OTP flow.
- **Context:** W1 found and fixed (commit fdeaa4bd, file-only, mutation-verified, no push/deploy/DB) a real chained PII/financial-data leak: `GET /api/referrers?code=|email=` returned a referrer's total_earned/total_paid/preferred_payout with zero auth (a referral code is public by design, it's the share link), and `GET /api/referral-commissions?referrer_id=...` had zero session check, exposing a referrer's full commission ledger including third-party client_name + booking price/date. Both are now fixed (financial fields stripped from the public lookup, commission-ledger branch gated behind the same referrer session token the OTP dashboard already uses). Side effect: the 5 legacy clone pages above never authenticate and relied on the now-closed public leak — they will show blank/undefined earnings and an empty commission list going forward. One of them (nycmaid) is still linked from NYC Maid's live marketing nav as "Referrer Login." W1 correctly did not touch this — NYC Maid is off-limits per standing order, and repointing 6 files is a broader UI migration outside its auth/authz lane.
- **Recommendation:** repoint the 5 clone pages to the already-built secure flow (top-level `/referral` + `/referral/[code]`) — no new backend work needed, just routing/link changes. Low risk since the destination flow is already live and OTP-gated.
- **Blast radius if left as-is:** the 5 legacy pages silently show blank earnings/empty ledger to real referrers (broken UX, not a security issue post-fix) until repointed or removed.
- **RESOLVED 2026-07-15 ~10:18.** Jeff approved the recommendation (migrate to the existing OTP flow). Dispatched to W2. Note: this entry's "NYC Maid is off-limits" caveat predates the 2026-07-14 ~19:00 clarification (RESUME-POINT.md session-10) that shared-platform code touching `nycmaid/`-tagged files is fine to resolve on technical merit — only the separate standalone NYC Maid repo is hands-off. W2 was told to include all 6 pages, nycmaid's included.

## 21:16 · referral_commissions schema ambiguity -- needs a read-only prod query before migration 066 (W1 finding)
- **Decision needed:** approve a read-only DB query to determine which of two incompatible referral_commissions table schemas is actually live in prod.
- **Context:** two different, incompatible referral_commissions shapes exist across migrations 008 (no booking_id) and 019 (the shape route.ts/post-adjustments.ts actually use), both applied manually with no tracking of which one is live on any given tenant. W1 built a real double-pay fix (compare-and-swap on referral_commissions PUT, file-only, commit 33ceed16) plus a proposed unique index (066) but cannot verify the index is safe to run without knowing the live schema.
- **Recommendation:** a single read-only query (check live columns on referral_commissions) resolves this. Reconcile-gate lane should run it once you approve DB read access, then 066 can be scoped to the correct schema.
- **Blast radius if run blind:** migration 066 could fail outright or silently create a constraint against the wrong columns.

## 18:18 · CRITICAL, currently live in prod — Telegram webhooks have zero real auth (W1, ops action needed)
- **Context:** all 3 Telegram webhook routes (owner bot, Jefe bot, per-tenant Selena/Yinez bots) authenticate ONLY by comparing chat.id from the raw unverified POST body against a stored owner chat id — no secret_token header check exists anywhere in the code that's actually on main. Chat IDs aren't secret. Anyone who POSTs a forged body with the right chat_id gets full owner-level agent access: Selena/Yinez has send_broadcast/approve_refund/mark_payment_received/update_booking/send_message_to_client with no enforced confirm=true (an attacker-controlled conversation can just skip the prompt-level "confirm first" guidance); Jefe's confirm-gated tools (rerun_cron, notify_tenant_owner) can be self-confirmed since the attacker controls both turns of the forged conversation.
- **This is not a branch-only finding — it's live in prod right now,** because the verification code was never merged to main (per LEADER-HANDOFF sec 10, the fail-closed telegram-webhook-auth.ts work from an earlier session never shipped).
- **Fixed (file-only, p1-w1, commit 5b669cf0):** added real secret_token verification, wired into webhook registration, gated all 3 routes. Deliberately fail-OPEN until TELEGRAM_WEBHOOK_SECRET is configured (fail-closed on deploy would 401 every legitimate bot instantly, before you complete the ops step below) — once configured, fails closed correctly. Mutation-verified, tsc clean, full suite green.
- **OPS ACTION NEEDED once this merges+deploys (outside any worker's scope, W1 flagged it explicitly):** set TELEGRAM_WEBHOOK_SECRET, then re-register the Jefe bot webhook + any tenant bots that already saved a token so Telegram starts sending the header. Note: an earlier session (session-6, ~15:12) already generated a secret and set it in Vercel prod, re-registering owner+jefe webhooks with it — worth checking whether that secret is still valid before generating a new one, or this fix may already start enforcing correctly for those two bots the moment it deploys.
- **Recommendation:** given the real money-actions reachable (approve_refund, mark_payment_received) and that this gap is live in prod today, worth prioritizing this merge+deploy+ops-step ahead of the general batch review.
- **Minor noticed-not-fixed:** webhooks/telegram/route.ts's GET handler has no auth and echoes OWNER_CHAT_ID + BOT_TOKEN length to any caller while spamming the owner's Telegram — lower severity now that chat_id isn't the auth mechanism, but still worth a follow-up pass.
- **18:32 — Jeff's own literal word ("yes") received in chat, executed.** Cherry-picked 5b669cf0 onto hotfix/telegram-webhook-auth-2026-07-13 off main, tsc clean + 17/17 targeted tests pass locally, opened PR #16, waited for full CI (typecheck+full suite+tenant-isolation+lint) to go green (4m45s), merged (05e264c5), Vercel auto-deployed to prod (Ready, 3m build). Live-verified: forged unsigned request correctly rejected 401.
- **18:52 — URGENT, possible live regression found during verification.** RESUME-POINT records session-6 registered the owner/jefe Telegram webhooks with Telegram using HMAC-derived per-scope secrets, not the raw TELEGRAM_WEBHOOK_SECRET. My deployed code checks the raw value directly, no derivation. Tested both interpretations of the pulled secret against the live endpoint, neither matched. If real, real Telegram messages to owner/jefe bots may be getting rejected right now, not just forged ones. Telegram's getWebhookInfo shows 0 pending/no error for jefe's webhook (reassuring but not conclusive — depends on whether anyone's messaged it since deploy). Asked Jeff to test by messaging either bot directly to confirm/rule out. Two fix paths ready pending his input: (a) patch code to match the real derivation, or (b) temporarily unset TELEGRAM_WEBHOOK_SECRET to restore fail-open — holding on (b) specifically since it's a prod env-var change, wants his explicit go even though it would be fixing my own deploy.
- **19:04 update — root cause confirmed, Jeff said "go" on Option A (19:02), execution paused mid-way on a real complication.** Jeff's plan: re-register all 3 webhook scopes with the raw TELEGRAM_WEBHOOK_SECRET as secret_token via setWebhook. Tried it — Telegram itself rejected the raw value with 400 "secret token contains unallowed characters" on all 3 scopes + all 6 tenant bots. Root cause found: the stored env var is 66 chars, not 64 — it has a literal 2-character backslash-n artifact appended (not a real newline). Telegram's secret_token field doesn't allow that character, so this value could never have been successfully registered as-is.
- **The complication:** the deployed code reads this env var raw with no cleanup, so registering Telegram with a cleaned-up 64-char value would just flip the mismatch the other way — the actual fix needs the stored Vercel env var itself corrected (strip the artifact), not just a Telegram-side re-registration. Reported this to Jeff and paused before touching the env var — same standard held all session for prod config writes, even to fix something from my own deploy.
- **19:15 — RESOLVED, fully verified live.** Jeff said "go" again. Corrected TELEGRAM_WEBHOOK_SECRET in Vercel prod to the clean 64-char value (removed + re-added), redeployed (fullloopcrm-7wqs4f5py, Ready), confirmed live: unsigned request → 401, correctly-signed request → 200. Re-registered all webhook scopes with the corrected secret: owner ok, all 6 tenant bots (nycmaid, nycroadsideemergencyassistance, debt-service-ratio-loan, landscaping-in-nyc, nyc-tow, fla-dumpster-rentals) ok.
- **Real side-finding caught and corrected during this:** JEFE_BOT_TOKEN is empty in prod. Owner and jefe are confirmed the same single physical bot (matches your earlier consolidation decision) — before I touched anything, that one bot's webhook was already pointed at the jefe endpoint. My first re-registration attempt (registering "owner" separately) repointed it to the owner endpoint, which would have broken your actual jefe workflow. Caught it via getWebhookInfo, reverted immediately back to the jefe endpoint with the corrected secret, verified live: unsigned → 401, signed → 200. Net state: same routing as before (jefe endpoint), now with a working, non-malformed secret gate.
- **Cleaned up:** all temp files containing the decrypted secret/tokens deleted after use.
- **Also noticed, unrelated to this fix:** TELEGRAM_OWNER_CHAT_ID is empty in prod, and the code still has 2 separate route files (owner + jefe) even though your 15:11 decision was to consolidate to one scope — that refactor was dispatched to W6 earlier but W6 was stood down before delivering it. Both are separate, non-urgent items.

## 18:15 · NYC Maid architecture status — needs your direct word, not a relay (DECISION NEEDED)
- **Context:** consultant relayed a standing-rule instruction that NYC Maid "is not active yet in the new architecture" and "not ready for cutover," directing the fleet to stop all NYC Maid work until you explicitly greenlight it.
- **Conflict:** this contradicts LEADER-HANDOFF.md section 9 ("NYC Maid is the protected flagship — only tenant on new SELENA, the reference case study, and revenue") and today's own session history (you directed a full nycmaid parity pass on the new platform earlier today, explicitly "copy+confirm, NO cutover" at that time).
- **Two readings:** (a) NYC Maid genuinely isn't live on the new platform yet, section 9 is stale and needs rewriting, or (b) the restriction is narrower — specifically the phone/voice cutover, which matches what I actually found in prod today (zero real call traffic on the telnyx-voice webhook) and is already tracked as the known Telnyx cutover blocker.
- **Status:** fleet is hands-off NYC Maid starting now either way (no cost to complying with the cautious read immediately). Not rewriting section 9 until you confirm which reading is correct — that's a cross-tenant architecture call, which per standing rule needs your own literal words, not a consultant relay.
- **Recommendation:** your call on which reading is accurate; I'll bank exactly what you say.

## 17:39 · session-8 tick — more real security fixes landed (tracking, same batch as the TOCTOU/race review)
- W1: OTP brute-force gap on 2 verify endpoints (pin-reset/route.ts verify_and_set, portal/auth verify_code) — send_code was throttled, verify was not; both now failClosed rate-limited. Fixed, file-only, p1-w1.
- W3: 2 real authz/IDOR gaps — client/preferred-cleaner + client/recurring never called protectClientAPI() (any client_id could read/edit another client's data or create bookings+SMS on their account); team-portal/messages never enforced its own 'messages.use' permission (any team_member_id could read/post in another cleaner's thread). Both fixed, file-only, p1-w3.
- W5: referral-commissions PUT double-credited referrer.total_paid on repeat/retry PUT with status=paid (no already-paid check) — real financial dup-payout risk. Fixed, file-only, p1-w5.
- No new decision needed yet — folding into the same merge+deploy batch review as the TOCTOU/race items above. Flagging so the accumulating weight stays visible.

## 17:42 · session-8 — HIGH: super-admin god-mode PIN rate-limiter fail-open gap (W4, distinct severity, flagging separately)
- **Context:** W4 swept every rateLimitDb() call site fleet-wide for the fail-open-on-DB-error gap. Found 3 auth-critical PIN logins missing failClosed that every other auth-critical endpoint fixed this session already had: admin-auth/route.ts (SUPER-ADMIN god-mode-any-tenant PIN + tenant-admin PIN — the highest-privilege login on the whole platform), team-portal/auth/route.ts (cleaner PIN), client/login/route.ts (client PIN).
- **Why this one is different from the rest of tonight's sweep:** the other findings tonight (OTP brute-force, authz gaps, injection) are each scoped to one tenant or one feature. A DB hiccup silently disabling the brute-force guard on admin-auth is platform-wide — that PIN unlocks every tenant at once.
- **Status:** fixed, file-only, p1-w4, commit e70a11df, 3 new regression tests, tsc clean, full suite green. Not pushed/merged/deployed.
- **Recommendation:** review this one specifically when you get to the batch — same gate (merge+deploy) as everything else, just flagging it shouldn't get lost in the sweep-class noise.

## 17:56 · session-8 — telnyx-voice inbound calls not being answered (functional, not security, worth separating)
- **Context:** W5 (8b3bf1ce) then independently W3 (438484ea) both found+fixed the same bug: the telnyx-voice inbound-call handler calls 2 required-tenant-id Postgres RPCs without passing tenant_id. Since neither function has a default, PostgREST can't resolve the call, the handler hits its own early-return before ever answering. W3's words: "this isn't a leak, it's a broken core feature."
- **Why flagging separately:** this is customer-facing on the flagship tenant (NYC Maid) — if this code shape is what's live in prod today, inbound calls may not be getting answered at all, not a theoretical risk. I have not verified live prod behavior from here (same limitation as the earlier telnyx-401 SMS finding).
- **Status:** fixed, file-only, 2 branches (p1-w5, p1-w3), not pushed/merged/deployed.
- **18:10 update — checked prod directly, NOT an active emergency:** queried comhub_active_calls + comhub_missed_call_sms for nycmaid's tenant_id in the real FL prod DB. Zero rows of any kind today. Most recent call activity in the table at all is 2026-05-21 (~2 months ago); the only inbound calls on record are a small cluster from +12122029220 (Jeff's own number per prior session notes) on 2026-05-08, reading as internal test calls, not real customer traffic. Total historical rows: 26, all in May.
- **Revised read:** this webhook is not currently receiving NYC Maid's real customer call traffic at all — separate from the p_tenant_id code bug itself, consistent with the already-tracked, never-completed Telnyx cutover from earlier sessions (the SMS side of the same cutover stalled on a 401 and was never finished). Not independently confirmed where NYC Maid's real production number currently routes — only that this table shows no live traffic hitting it.
- **Recommendation:** normal batch review, not emergency deploy. Fixed file-only on 2 branches, real bug, but not costing missed calls right now based on the data.

## 16:19 · client/verify-code.ts duplicate client rows (DECIDED by Jeff 16:36, dispatched — tracking only)
- **Decision:** Jeff confirmed directly in the leader terminal — add a UNIQUE constraint on `clients(tenant_id, email)` (scoped by tenant, not a bare email unique) to close the duplicate-row gap.
- **Dispatched:** W3 authoring (1) a dedupe pre-check/script for any existing duplicate rows (not run), (2) the guarded ADD CONSTRAINT migration, (3) an update to `client/verify-code.ts` to catch the resulting 23505 and return the existing client instead of a raw 500, with tests.
- **Status:** file-only, not applied to prod. Applying the migration is still gated on Jeff's separate DB-write go-ahead when ready.

## 16:27 · 6 real TOCTOU/race fixes ready for merge+deploy review (tracking, same gate as Q3)
- **Decision:** none needed yet — flagging so the accumulating weight is visible before a batch review. All file-only, not applied to prod.
- **Context:** W3's audit this session closed: CRITICAL duplicate-tenant-creation race, 2 HIGH duplicate-booking/recurring-series races (one public-endpoint-reachable), 1 MEDIUM quote-convert race, 1 HIGH bank-transaction double-credit race, 1 HIGH journal-entry double-posting race spanning 9 ledger call sites. Plus the earlier stripe-webhook money-race fix (4 sites). 2 migration files prepared, not applied. W4 independently reviewed all of them for correctness.
- **Recommendation:** review as one batch alongside Q3 when you're ready — these are all genuinely file-only and reversible until you say merge.
- **Blast radius if left unmerged:** the races stay live in prod until deployed; if merged carelessly without the pending claim-release-on-error fix (W3 closing now), a failed retry could leave a row stuck needing manual DB repair — flagging so that fix lands before this batch is truly ready, not before you decide to act.
- **Update 17:10 2026-07-17 (W4), HIGH — new instance, the sharpest one in this batch:** the shared rate-limiting primitive (`rate-limit-db.ts`) behind **all 17** `failClosed:true` auth-critical endpoints — admin login, client login, team-portal login, referrer OTP, PIN reset — had a plain TOCTOU race with zero locking. Concurrent requests to the same bucket could all read the same pre-race count and all get allowed, letting an attacker blow past the 5-attempts/window brute-force cap by firing guesses concurrently instead of sequentially, on any of those 17 login surfaces. Fixed with a new `rate_limit_check_and_record` RPC using `pg_advisory_xact_lock` per bucket — ships safely ahead of the DDL via a fallback to the old (racy but functional) path, self-upgrades once the migration runs. Migration file: `2026_07_17_rate_limit_check_and_record_atomic_PROPOSED.sql`, not applied.
- **Update 14:13 2026-07-17 (W4):** re-verified `create-tenant-from-lead.ts`'s duplicate-tenant-creation race specifically — still a plain read with no atomic claim, 2 concurrent `checkout.session.completed` deliveries can still both fully provision a tenant for one paying customer. Its migration (`2026_07_16_partner_requests_conversion_claim_column_PROPOSED.sql`) has been prepared and unapplied for 24h+ — W4 flags it as the highest real-money blast radius of any pending PROPOSED migration right now, worth prioritizing within this batch specifically.

## 15:11 · Telegram owner/jefe bot consolidation (DECIDED by Jeff, dispatched — tracking only)
- **Decision:** owner and jefe are the same physical bot in prod (verified byte-identical token) — Jeff decided: consolidate to ONE scope in code rather than provision a second bot. Reasoning given: no observed issues running as one bot so far, a second bot adds permanent operational overhead (two BotFather identities, two token rotations, two webhook registrations) for no observed gain.
- **Jeff's spec:** single `TELEGRAM_BOT_TOKEN`, single webhook URL, route internal handling by message content (command prefix or chat_id) rather than by scope. Remove `JEFE_BOT_TOKEN` references. Update secret verification to use the single scope's derived secret.
- **Status:** dispatched to fleet as file-only refactor + tests. **Merge to main + deploy still needs Jeff's separate gate approval** when the refactor lands — not auto-actioned.
- **Blast radius if wrong:** low-medium — the bot already behaves as one bot in practice; refactor is behavior-preserving by design, verified via tests before landing.

## 15:11 · Real money-race in stripe-webhook (HIGH, found by W3)
- **Decision needed:** none yet — file-only fix proposal in progress (not applied). Flagging now so it's visible before Q3/deploy planning.
- **Context:** booking-payment path has zero idempotencyKey repo-wide + non-atomic select-then-insert on `payments.stripe_session_id` — retry-overlap can double-pay a cleaner real money via NYC Maid's instant-payout path. Same non-atomic-guard shape also hits quote-deposit (ledger double-post) and job-conversion (dup job rows).
- **Recommendation:** apply the same atomic UPDATE...WHERE...RETURNING pattern already proven in this file for the prospect-signup race, to all 3 other paths. Fix proposal in progress on p1-w3, file-only, not applied.
- **Blast radius if wrong (i.e. if left unfixed):** HIGH — real money, real cleaner payouts, race is retry/concurrency-triggered so it's a live risk not a hypothetical.
- **17:52 update (W4, independent confirmation on p1-w4):** root cause confirmed precisely — insert error on the real UNIQUE(stripe_session_id) constraint was never checked, so the losing delivery fell through to a real Stripe Connect transfer anyway (literal double-payout, not just a theoretical race). Same fix pattern applied on p1-w4 (23505-as-claim + compare-and-swap on the deposit path), commit dbddb325, file-only. **Merge caveat from W4 directly: if main or p1-w2 already carry a different fix for this same webhook race, this needs reconciling at merge time, not a clean cherry-pick** — worth knowing before batch review.
- **18:03 update (W4) — traced to the actual root cause, bigger than just the webhook:** every ledger poster (deposit/refund/chargeback/commission-accrual/commission-payment, 5 functions across post-revenue.ts/post-labor.ts/post-adjustments.ts) guards against double-posting with an application-code SELECT before insert — there is no DB-level constraint backing it at all. Any concurrent caller for the same tenant/source/source_id, not just Stripe webhooks, can double-post real accounting entries. Fixed at the root: a proposed migration (file-only, NOT applied) adds a partial UNIQUE INDEX on journal_entries(tenant_id, source, source_id), plus postJournalEntry() updated to treat a DB-level conflict as already-posted across all 7 call sites. This is the definitive fix — the migration needs your approval before I run the DDL, the JS-side change alone does nothing until that lands.

---

## Q1 — 20:37 · fladumpsterrentals.com is DOWN (DNS)
- **Decision:** Approve a DNS repoint / registrar fix for `fladumpsterrentals.com` (I never touch DNS — you or registrar).
- **Context:** Live probe 2026-07-11 20:30 — 29/30 real tenant domains return 200; **fladumpsterrentals.com = 000, nameservers unreachable** (`dig` times out, "no servers could be reached"). Same failure class as toll-trucks-near-me (dead zone). No worker blocked; it's an infra/registrar action. The tenant (`fla-dumpster-rentals`, industry=dumpster, 4 active services) is dark to customers.
- **Recommendation:** Repoint the domain's nameservers to the working DNS (Vercel/registrar) as with the other FL-team domains. Reasoning: it's a pure DNS-zone failure, code is fine (other 29 up).
- **Blast radius if guessed wrong:** low — DNS-only; worst case a wrong record = still-down, reversible.

## Q2 — 20:37 · toll-trucks-near-me DNS repoint (P0-5)
- **Decision:** Approve DNS repoint of toll-trucks-near-me to Vercel.
- **Context:** GoDaddy delegates to a cancelled SiteGround zone → SERVFAIL; site dark. Long-standing (from LEADER-HANDOFF). Not code. No worker blocked.
- **Recommendation:** Repoint NS to Vercel. Low risk, reversible.
- **Blast radius:** low, DNS-only.

## Q3 — 20:37 · PART 0 RELEASE PIPELINE — merge WAVE-2 to main + prod DB + deploy — ✅ RESOLVED 2026-07-15 ~15:15, see MASTER-TODO-LIST.md Part 0 reconciliation note for full detail (merged to main, all migrations run+verified, security bundle deployed live; resolver-flip sub-item deliberately still gated, its own separate ask)
- **Decision:** Approve the full Part 0 sequence (git push to main + prod DB migrations + deploy). This is the big one — 3 of the gates at once, in strict order.
- **Context:** ~22 WAVE-2 isolation/auth fixes + both backfills are branch-real only; **prod carries every WAVE-2 hole today.** 31/130 punch items are CODE-complete and gated behind this. Sequence (must be in order): re-integrate wave2 → rebuild green → merge to main → DB migrations (055 done; 058/059/060/061/062/owner_phone/pricing pending) → env (TELEGRAM_WEBHOOK_SECRET + re-register) → deploy security bundle → resolver flip w/ 24-48h watch. Workers affected: all 4 lanes feed this.
- **Recommendation:** Approve, but **staged, not one push** — Phase A low-risk, B resolver-flip w/ assert-guard, C auth-behavior (after owner_phone backfill), D webhook idempotency (after Telegram secret). Reasoning: phased = you know which wave broke something without a bisect.
- **DO-NOT-SKIP:** (1) owner_phone backfill BEFORE booking-owner deploy or **19 tenants lock their owners out** (verified 19 NULL). (2) TELEGRAM_WEBHOOK_SECRET + re-register BEFORE that deploy or **all bots go dark**. (3) extend pricing PASS C allowlist + add nycmaid guard BEFORE pricing backfill or flagship checkout math changes.
- **Blast radius if wrong:** HIGH — touches all 22 brands, money paths, auth. This is why it's staged + gated.

## Q4 — 20:37 · ENGINE CUTOVER decision (architectural, multi-tenant)
- **Decision:** Keep the split (non-nycmaid customers on legacy engine) OR cut non-nycmaid customers over to the new SELENA agent.
- **Context:** `/api/chat` + `/api/webhooks/telnyx` route only nycmaid to new SELENA; **every other tenant's web+SMS customer traffic hits `selena-legacy.ts`.** The new persona pipeline (19 authored configs) is **dark for non-nycmaid customers.** Also: 15 of 22 active tenants have empty/near-empty `selena_config` (DB-verified) — on the new agent they'd get a generic persona; F2/F3 latent bugs (dead exterminator config, dropped price numbers) only bite after cutover.
- **Recommendation:** **Keep the split until** F2/F3 fixed + the 15 empty configs authored. Reasoning: cutting over now would degrade non-nycmaid customer AI (generic persona, no prices). Sequence: author configs → fix F2/F3 → cut over per-tenant.
- **Blast radius if wrong:** HIGH — affects every non-nycmaid tenant's customer-facing AI.

## Q5 — 20:37 · Remove 6 leftover w1-e2e-* test tenants (prod DB write)
- **Decision:** Approve deleting 6 `w1-e2e-*` test tenants still `active` in tenant_domains.
- **Context:** E2E test tenants from a prior run (`w1-e2e-cleaning/handyman/hvac/junk-removal/pest/towing-*.fullloopcrm.com`) never cleaned up; LEADER-HANDOFF claimed test tenants auto-cleaned. They're on the fullloopcrm subdomain, not customer-facing brands. Prod DB write = gated.
- **Recommendation:** Delete after confirming they carry no real bookings/money (I'll author the safe-check query + delete script as prep, not run it).
- **Blast radius if wrong:** low-med — could delete a real row if a slug collides; the pre-delete safe-check query prevents that.

---
## Q3 UPDATE — 02:55 · Stage 0 re-integration DONE-ish (leader ran it)
- **integ/wave2-2026-07-11 @ fbe36df0**: all 4 worker branches merged (W4/W1/W3/W2), all conflicts resolved (security KEEP-BOTH unions, SEO→p1-w3, tests unioned). **tsc clean at merge. Vitest 945/996 pass.**
- **NOT fully green:** 11 behavioral auth-route tests fail (KEEP-BOTH hybrid routes vs each branch's exact test expectations) + build needs Vercel env (not a merge defect). Precise green-up steps in RESUME-POINT.md CHECKPOINT-2.
- **Your gate unchanged:** push-to-main still waits for you. Recommend: let a fresh-context leader finish the 11-test green-up first, THEN you review + merge. The merge work (the hard part) is done and committed.

---
## Section P gated items (added 2026-07-12 ~11:31)
- P12 (GATE): Backup restore drill — needs prod backup access; authorize when you want it run. Ties to DR runbook.
- P13 (GATE, Jeff sign/attest only, no build): PCI SAQ-A self-attestation via Stripe (~30 min).
- P14 (GATE, Jeff sign/attest only, no build): Sign DPAs with Stripe, Twilio/Telnyx, Supabase, Resend, Anthropic/xAI, Vercel.

## MUST-REVIEW — added 12:21 2026-07-12 (session-4)
- [ ] **DEPLOY the SMS opt-out TCPA hotfix** (Jeff-gated: deploy). W5 is applying the fix (clients.sms_opt_in -> canonical clients.sms_consent in the apology-batch) + a test proving opted-out clients are skipped, file-only on p1-w5. **LIVE TCPA exposure** (up to $1,500/text to an opted-out client) — Desktop consultant + you both said patch-now, not behind Q3. WHEN W5 REPORTS DONE: review the diff, then deploy on a hotfix branch (small single-file blast radius, independent of Q3). Leader will post the commit here + in the chat when it lands.

## SUCCESSOR CONTACT RECORDED (2026-07-12)
- Ashton Tucker, 212-202-9201, ashtonjtucker@icloud.com = overall platform successor. Saved to SUCCESSOR-CONTACT.md + LEADER-HANDOFF.md. Hole #20 (Section Q-S5) full successor docs still open.

## 16:55 2026-07-12 SECURITY (HIGH) — live cross-tenant IDOR in GET /api/selena?convoId
- convoId branch (src/app/api/selena/route.ts ~L14-21) reads sms_conversation_messages by conversation_id ONLY — no tenant scope. Operator authed as tenant-A passing tenant-B convoId reads tenant-B full SMS transcript (name/phone/address/email PII).
- Found by W4; witness test eec486b7 on p1-w4 pins it (RED until fixed). Fix dispatched to W4 (16:55) as an on-branch commit.
- GATED: the route fix lands on p1-w4; DEPLOY to prod is Jeff-only. Deploy alongside / after the TCPA hotfix.

## 18:04 2026-07-12 PROD (HIGH) — flagship tenant nycmaid live domain missing API routes
- www.thenycmaid.com serves from a STALE Vercel deployment: /api/tenant/public + /api/health return generic Next.js 404 (x-matched-path:/404); static pages serve from ~8h-old edge cache (x-vercel-cache HIT). Other tenants (thefloridamaid, consortiumnyc) resolve correctly.
- Theory (W4, read-only curl evidence): thenycmaid.com domain is pinned/aliased to an older deployment that predates these API routes, OR not attached to the current platform deployment. Any nycmaid client code hitting those APIs is silently broken NOW.
- ACTION (Jeff): check Vercel project domain->deployment binding for thenycmaid.com / www.thenycmaid.com. Not a code fix, not security, no cross-tenant bleed. Full report: /tmp/w4-report-20260712-180037.md (W4 also authoring a durable copy).
- MED: /api/tenants/public (plural) missing from middleware isPublicRoute -> 307 to sign-in (low impact, /apply is 410'd). LOW: subdomain tenant routing has no DNS (infra gap).

  - LEADER UPDATE (18:07): investigated read-only. thenycmaid.com is NOT on the fullloopcrm platform Vercel project (CLI authed as fullloopcrm has no access to it). It's on a separate account/project. Leader CANNOT fix the binding without access to that account. Needs Jeff to fix in that Vercel account, or tell leader which team to auth.

---

## STATUS NOTE — 2026-07-12 18:20 · Fleet hit diminishing returns (NOT a gated decision)
- **What:** All 6 lanes have drained their real backlog. This session they finished: branch changelogs (p1-w1..w6) + merge-conflict dry-runs vs origin/main (artifacts in `deploy-prep/conflict-risk-p1-wN.md`; p1-w2/w5 ZERO conflicts, p1-w4 has 1 real conflict in `platform/src/app/api/t...`). The two security PRs (#14 TCPA, #15 selena IDOR) and the nycmaid Vercel binding are all gated on YOU.
- **Action taken:** I set the drained lanes to explicit IDLE (no self-assigned make-work) rather than churn fabricated tasks that burn Sonnet/weekly usage for zero product value.
- **What unblocks them:** you merging #14/#15, deciding Q3/Q4, or handing me a new lane of real work. Until then the fleet correctly sits idle.

- 19:48 W3 extended cross-tenant suite (+13 tests, 17331cbc) re-confirms the selena ?convoId IDOR (already fixed in PR #15) and adds booking[id]/portal-bookings[id] BREAK coverage on p1-w3. No NEW live exposure beyond PR #15; test coverage only.

## PARITY DECISION (added 10:44 2026-07-13, W1 email parity)
- **Contact phone drift:** FL nycmaid emailWrapper + 13 templates use **646-490-0130**; live nycmaid uses **212-202-8400**. Baked in since first port commit (ties to FL nycmaid site sales-vs-support split, no tenant-config field drives it). W1 did NOT auto-revert. **DECISION:** is 646-490-0130 a real staffed line for nycmaid email contact, or should it revert to 212-202-8400 to match live? One word and W1/W3 aligns it.

## CUTOVER BLOCKER ROOT-CAUSED (11:00 2026-07-13) — Telnyx 401
- Jul-7 cutover died here. NOT a code bug: TELNYX_PUBLIC_KEY is one GLOBAL env var, Telnyx signing keys are per-ACCOUNT. nycmaid's valid webhook requests 401'd against the wrong account's key. (nycmaid's own webhook never verified sigs — no behavior to port.) W6 commit 7f535968 reproduces it.
- FIX (W5 building, file-only): per-tenant tenants.telnyx_public_key column + verifyTelnyx() prefers tenant key. JEFF ACTION at cutover: run the migration + set nycmaid's real Telnyx public key value on the tenant. Then the SMS repoint that failed Jul-7 will verify.

## 20:58 · CRITICAL, currently live in prod — team_members.pin exposed to any authenticated role
- **Decision needed:** which roles should be able to see teammates' team-portal PIN (the sole team-portal login credential)? Candidates: (a) gate PIN visibility to team.edit only (owner/admin by default) and strip pin from all list/aggregate endpoints, keeping it only on the single-member detail view; (b) never return raw pin after initial issuance — replace persistent display with an owner/admin-only "reissue PIN" action.
- **Context:** GET /api/team, GET /api/team/[id], GET /api/dashboard all leak team_members.pin via select('*') with zero permission gate (any authenticated role including lowest 'staff'). GET /api/cleaners is gated on team.view but staff has team.view by default, still leaks. dashboard/team/[id]/page.tsx intentionally renders the PIN today for legit admin new-hire workflows — this is why it's not a 1-line strip. Impact: staff can harvest any coworker's PIN -> log into their team-portal session -> falsify attendance, view earnings/PII, message as them. Full writeup: flwork-p1-w4/deploy-prep/w4-hr-pin-exposure-and-deals-quotes-rbac-gap-audit.md (Finding 1; Finding 2 in same doc, deals/quotes RBAC, already fixed this session).
- **Recommendation:** option (a) is the smaller behavior change (only removes visibility from roles that shouldn't have had it) — leader's lean, not acted on.
- **Blast radius if wrong:** picking too permissive a gate leaves the credential-harvesting path open; picking too strict breaks the legitimate new-hire-PIN-handoff workflow admins use today.
- **Update 00:32 — do not treat as resolved:** p1-w3 committed (0f5e01ec) a team.view gate on GET /api/team + GET /api/team/[id], despite standing leader instruction to leave these routes untouched pending your decision. Since staff has team.view by default, this does NOT close the exposure for the role that mattered — it's incomplete, not a fix, and not something to build on. Left in place (not reverted, not worth the churn), but the decision above is still fully open and unaddressed. Workers re-instructed to stay off these routes.
- **RESOLVED 2026-07-15 ~10:18.** Jeff picked option (a). W1's commit `052a3f21` (made before this was flagged to him — caught after the fact, reviewed, and approved) actually closes the gap the 0f5e01ec attempt missed: it strips the `pin` field from the GET /api/team + GET /api/team/[id] list response body itself (not just a permission gate), so it doesn't matter that staff has team.view by default — the field isn't in the payload. Single-member `[id]` detail view still returns it, preserving the legit new-hire PIN-handoff workflow. Dispatched W4 (10:18) to apply the identical fix to the third leaking route, GET /api/dashboard. File-only, no push/deploy/DB on any of this.

## 20:58 · Product gap — jobs feature has no RBAC permission defined at all
- **Decision needed:** what should staff vs. manager be allowed to do to a job (view/edit/delete/reassign)? rbac.ts has no permission concept for jobs at all, unlike every sibling module.
- **Context:** found during broad-hunt (W3). Not a quick fix — needs a product decision on the permission shape before any code change, same pattern as the deals/quotes RBAC gap (which Jeff already resolved by reusing sales.view/sales.edit — jobs may or may not want its own permission pair).
- **Recommendation:** dispatching a worker to draft a proposal (options doc, no code) mirroring the deals/quotes writeup, so there's something concrete to react to next time you're free.

## 20:58 · Low urgency — dead-code cross-tenant auth landmine (nycmaid/auth.ts + wash-and-fold clone)
- **Decision needed:** delete createClientSession/verifyClientSession (currently unreachable, never called) or fix them now (add tenant binding, keep the code).
- **Context:** both functions share ADMIN_PASSWORD with no tenant binding. No live exposure today since nothing calls them, but a landmine if someone wires them up later without noticing. This is shared-platform code under a nycmaid/ path, not the separate NYC Maid repo, so it's in-scope per the settled hands-off clarification.
- **Recommendation:** delete — removes the landmine outright with near-zero downside since the code has no callers today. Leader's lean, not acted on.
- **Update 16:11 2026-07-17 (W4):** expanded from 2 files to 6 — audited every per-tenant site-clone `_lib/auth.ts` (nyc-mobile-salon, wash-and-fold-{nyc,hoboken}, the-nyc-interior-designer, the-home-services-company, the-nyc-exterminator). Worst instance: the-home-services-company's `isAdminAuthenticated()` checks a static **unsigned** cookie string with zero signature — a trivial full bypass if it were ever wired up. All 6 confirmed via grep to be 100% dead code today (zero live importers of any admin-session export), so still no live exploit path — same delete-vs-fix decision as above, just now with 6 files and one notably worse example, not touched unilaterally.

## 20:58 · RESOLVED, no action needed — Telegram webhook secret activation
- Verified directly via `vercel env ls production`: TELEGRAM_WEBHOOK_SECRET is set (encrypted, 1 day ago). JEFE_WEBHOOK_SECRET does not exist as a separate var, consistent with the already-known owner/jefe-same-bot finding. Closing this out — was raised as a possible gap by multiple workers but isn't one.

## 21:06 · Business-logic question, not security — payroll mark-paid clears entire worker balance
- POST /api/finance/payroll's "mark related bookings as paid" step (route.ts:137-142) marks EVERY completed booking for that worker as paid, ignoring the payment amount or period supplied — a partial payroll payment silently clears the worker's whole outstanding balance from future payroll-prep runs. Pre-existing (aecbdcb9), no live UI caller found (API-only today). Could be intentional (pay-total-clear-slate) or a real accounting bug. W2's call to flag rather than rewrite unilaterally.

## 21:53 · Low urgency, flagged not fixed — /api/admin/cleaner-availability auth mismatch
- Route combines requireAdmin() (super-admin-only token) with getTenantForRequest() — a pre-existing mismatch that may lock out ordinary tenant staff who use it via BookingsAdmin.tsx. W1 left untouched as out-of-scope for its RBAC pass, flagging for you rather than guessing the intended access level.

## 22:50 · IMPORTANT for Q3 migration stage — journal-entries migration re-opens a closed vuln
- p1-w1's copy of the journal-entries dedup migration (064_unique_journal_entries.sql) does CREATE OR REPLACE FUNCTION post_journal_entry(...) ending in GRANT EXECUTE...TO authenticated, service_role — this silently undoes an earlier migration (060_lockdown_secdef_rpcs.sql)'s REVOKE, which had closed a cross-tenant journal-forgery hole (post_journal_entry is SECURITY DEFINER, bypasses RLS, took tenant_id as a plain unchecked arg). If migrations run in numeric order, 064 re-opens exactly what 060 closed. W1 fixed p1-w1's copy (REVOKE + GRANT service_role-only, search_path re-pinned) but flagged that p1-w2 (061_unique_journal_entries.sql) and dated files on p1-w3/p1-w4 independently wrote the same fix with likely the same regression — needs checking before picking which copy goes into the real migration set for prod.
- **Update 22:52:** scope now confirmed — W4's copy (`platform/src/lib/migrations/2026_07_13_journal_entries_dedup_constraint_PROPOSED.sql:110`) has the identical regression, fix dispatched. W2's (`061_unique_journal_entries.sql`) and W3's (`2026_07_13_journal_entries_source_unique.sql`) only add an index, don't redefine the function — confirmed clean, no action needed on those two.
- **RESOLVED 22:58:** both affected copies (p1-w1's 064_unique_journal_entries.sql and p1-w4's 2026_07_13_journal_entries_dedup_constraint_PROPOSED.sql) now fixed and confirmed matching (REVOKE FROM authenticated/PUBLIC + GRANT service_role only + search_path re-pinned). p1-w2/p1-w3 confirmed clean (index-only, no function redefinition). No open action remains here — just don't lose this when the 8-migration list gets finalized, use one of the now-fixed copies.

## 23:14 · Flagged, not fixed (bigger than file-only) — email-monitor auto-marks bookings paid on unverified inbound email
- lib/email-monitor.ts's detectPaymentEmail() is subject/body-heuristic only, not sender-verified — a spoofed email matching the pattern could falsely mark an arbitrary booking as paid. W3's call: worth a real look, not a quick patch. Same pass also closed a related ILIKE-wildcard escape gap and a missing rate-limit on /api/inquiry (file-only, already fixed).

## 23:43 · CRITICAL, FIXED 23:44 — comhub email-backfill could have corrupted cross-tenant customer data
- POST /api/admin/comhub/email/backfill hardcoded the single env-based IMAP mailbox (nycmaid's inbox) and tagged every imported email with whatever tenant is currently impersonated. Only reachable by global super-admin token (curl-only, no UI button), so not internet-exploitable, but a real landmine. **Fixed 23:44 (W4):** route now resolves the calling tenant's own IMAP profile via resolveTenantMailAccount(), matching cron/comhub-email's existing convention; 500s with a clear error instead of silently pulling nycmaid's mailbox if a tenant has no profile creds. File-only, no push/deploy/DB. Full audit: flwork-p1-w4/deploy-prep/w4-comhub-email-backfill-cross-tenant-leak-audit.md.

## 05:12 2026-07-18 · Flagged, not fixed -- owner/admin Telegram bots hardcoded off nycmaid, no status check (item 38, W2)
- telegram/route.ts and telegram/jefe/route.ts are both hardcoded to the nycmaid tenant with no tenantServesSite() gate (same class as item 30/ComHub and this session's now-fixed telnyx-voice + comhub-email status-gate gaps), but unlike those, access is chat-ID-allowlisted admin-only, not public.
- Decision needed: is admin-only chat-ID allowlisting sufficient risk coverage on its own, or should these get the same status gate as the public-facing surfaces? W2's call: flag, not guess, same reasoning as item 30.

## 06:04 2026-07-18 · Judgment call, not fixed -- should cron/backup gate on tenantServesSite()? (W2)
- Same tenantServesSite() sweep found cron/backup runs on every tenant regardless of status. Unlike the other 16 crons closed this session (messaging/booking/financial writes -- clear-cut "don't act on a dead tenant"), whether a SUSPENDED/CANCELLED tenant's data should still get backed up is a real data-retention question, not a bug.
- Decision needed: should suspended/cancelled/deleted tenants keep getting backed up (retention/compliance safety net), or should backup also gate on tenantServesSite() like everything else? W2's call: flag, don't guess -- reversing this later is easy but the retention implication (losing backup coverage for a tenant you might reinstate) is not something to guess at.

## 06:35 2026-07-18 · GATE 2 (prod DB write) -- migration prepared, awaiting your go: seo_run_detection() status gate (W2)
- File: 2026_07_18_seo_detection_status_gate.sql (on p1-w2, file-only, not applied). Gates the DB-side seo_run_detection() RPC on tenant status (mirrors the same NON_SERVING_STATUSES check already applied to every other tenantServesSite() fix this session), fail-open for tenant_id null and unknown status. Closes out the last unfixed piece of this session's full seo-* pipeline sweep (11 crons, 8 files already fixed file-only).
- Needs your explicit word to apply -- this is a prod DB write, gate 2. Once you say go, I'll run it directly (leader-only prod-write execution).

## 06:42 2026-07-18 · Flagged, not fixed -- item 33: 3 bespoke tenants carry cross-contaminated static domain-ownership lists (W2)
- wash-and-fold-nyc, wash-and-fold-hoboken, nyc-mobile-salon each ship their own dead, DB-un-synced _lib/domains.ts + _lib/lead-filters.ts implementing a static owned-domain list instead of the shared getOwnedDomainSet(). wash-and-fold-nyc's list is a near-verbatim copy of hoboken's (93/96 domains identical, including Hoboken/NJ/Long-Island-branded domains NYC has no claim to).
- Confirmed zero live production impact (lead-filters.ts has zero importers repo-wide, the real attribution.ts analytics engine doesn't use any of it). Not code-fixed because correcting the data requires knowing which of the 93 shared domains actually belong to which tenant -- ground truth W2 has no DB access to verify. Fabricating a corrected list would be guessing.
- Decision needed: who has the real domain-ownership ground truth to correct these 3 lists (or should they just be deleted in favor of the shared getOwnedDomainSet(), same as the dead-resolver-twin fix earlier this session)?

## 06:42 2026-07-18 · Judgment calls, not fixed -- seo-* pipeline verify-revert/alerts/health (W2)
- Same seo-* lib-layer sweep that closed 8 files on the tenantServesSite() class flagged 3 more spots (verify-revert, alerts, health) as judgment calls rather than clear-cut bugs, without further detail supplied in the channel line. Full specifics are in W2's own gap-doc (deploy-prep/w2-*), not restated here to avoid guessing at content I haven't verified myself.
- Flagging so it doesn't get lost; recommend pulling the actual gap-doc content before deciding, since I don't have the specifics to summarize accurately.

## 06:49 2026-07-18 · Missing-feature backlog (new track) -- tenant owners have zero self-serve domain configuration (W2)
- dashboard/websites/page.tsx tells every tenant owner "Contact your admin to configure your domain and publish" -- this is not stale copy, it's accurate: dashboard/settings/page.tsx has no `domain`/`domain_name` input anywhere (grepped both files), so an owner has no UI path to attach their own custom domain. Today this is 100% admin-mediated (admin/websites or admin/businesses/[id]).
- Notable half-built piece: PUT /api/settings (the tenant's OWN settings route, gated only on settings.edit, not platform-admin) already accepts and collision-guards a `domain` field end-to-end (normalize + findDomainOwner + cache-bust) -- that code path is reachable today by any tenant owner via direct API call, just never wired to a UI control. Effectively defense-in-depth on a route with no live UI caller for that field, same class as the isOwnedReferrer finding logged earlier this session.
- Decision needed: is admin-mediated domain setup the intended permanent design (DNS/Vercel attachment plausibly needs admin coordination anyway), or should self-serve be built? Not a quick fix either way -- self-serve would need DNS instructions UI, a verification flow, and a decision on whether to auto-call Vercel's domain-attach API from the tenant's own session (security/abuse surface, since PUT /api/settings has no platform-admin gate).
- Not acted on -- W2's call: this is a product-scope decision, not a bug. Ruled out three adjacent leads before landing on this one (confirmed via cross-branch check, not guessed): admin/websites domain-management UX (add-only, no remove) was already found+fixed on p1-w1 (commits 1b81484c/94deba85, not yet merged into this branch); the admin onboarding checklist's manual DNS-verified toggle is intentional human-checklist tooling, not a bypassed automated check; TENANT_DIVERGENCE silently darkening a tenant with no alert turned out to already be covered by the existing tenant-health "Fortress cron", which Telegram-alerts the owner on exactly this failure mode.

## 07:47 2026-07-18 · GATE 2 (prod DB write) -- backfill prepared, awaiting your go: payroll_payments.status never-set-paid (W1)
- File: backfill.sql on p1-w1 (deploy-prep/w1-payroll-payments-status-never-set-paid-out-omitted-2026-07-18-0744.md), file-only, not applied. Backfills payroll_payments.status='paid' for historical manual Record Payment rows that were never set (the code-side bug is already fixed on the branch).
- Context: without this backfill, historical manual payments keep reading as unpaid in payroll-prep's balance-owed stat, real risk of re-paying a contractor who was already paid manually.
- Needs your explicit word to apply -- prod DB write, gate 2.

## 08:49 2026-07-18 · Ops action, not a code fix -- Supabase 'uploads' bucket has no server-side file-size limit (W4)
- Every createSignedUploadUrl-based upload route (5 routes, 2 fully public/unauthenticated) declares a per-type maxSize in app code, but the client PUTs straight to Supabase Storage bypassing Vercel's 4.5MB body limit -- createSignedUploadUrl has no size parameter, so the app never sees the bytes until after the fact.
- W4 fixed post-hoc size verification (check-then-delete-if-oversized) on the 3 routes with a live consumer of the resulting URL; 2 more routes (lead-media, team-portal/photo-upload) have the same gap with no consumer to hook a check into yet, documented not silently dropped.
- Real root fix: set a bucket-level fileSizeLimit on the 'uploads' bucket in the Supabase dashboard (Storage settings, not a code change). This is the actual server-side enforcement; the app-layer checks above are a stopgap for routes that read the object back.

## 08:50 2026-07-18 · GATE 2 (prod DB write) -- migration prepared, awaiting your go: client_properties primary-race backstop (W1)
- File on p1-w1, file-only, not applied: dedupe-first migration + partial unique index on client_properties(client_id) WHERE is_primary, matching the backstop tenant_domains already has.
- Context: closes a real 0-or-2-primaries race (concurrent set_primary calls or concurrent first-property inserts), reachable live via the client portal and Selena's update_account SMS tool. App-layer fix (atomic RPC) already landed file-only on the same branch; this migration is the DB-side backstop for the same class.
- Sibling note (not fixed, flagged): client_contacts has the identical app-layer race already fixed but still lacks its own DB backstop -- separate table, separate migration, out of scope for this fix.
- Needs your explicit word to apply -- prod DB write, gate 2.

## 09:36 2026-07-18 · GATE 2 (prod DB write) -- migration prepared, awaiting your go: seo_issues.notified_at marker (W3)
- File: 2026_07_18_seo_issues_notified.sql on p1-w3, file-only, not applied. Adds a notified_at marker column so the new daily seo-alert-digest reports each open not_indexed issue once, not repeatedly.
- Context: this is part of the seomgr alert-path fix you asked for this morning -- closes the gap where seomgr detects real indexing issues but nothing ever tells you. site_down already alerts in real-time via the existing Fortress cron, no migration needed there. This one is specifically for the digest.
- Needs your explicit word to apply -- prod DB write, gate 2.

## 12:53 2026-07-18 · Flagged, not fixed -- possible connection-race under burst concurrency returns empty (not error) data (W4)
- Found while investigating/fixing the /admin slow-load report (12:37 order): the original 10-way-concurrent cron-check query burst intermittently (1 of 4 trials, all fresh-process first-call bursts) returned `{data: [], error: null}` -- empty, not an error -- for cron types that have real historical rows. Confirmed not a data problem by immediately re-querying sequentially and getting the correct row back. Looks like a connection-establishment race at the client/undici level under concurrent burst load, not something W4 could fully root-cause or safely fix further within task scope.
- Practical impact: on a cold hit, /admin could occasionally show a false "cron X has gone silent" flag that isn't real. The fan-out fix just committed (91def53f, batches 10 concurrent queries down to 1 for this group) should reduce how often this is hit but doesn't guarantee elimination -- it shrinks the burst, it doesn't fix the underlying race.
- Not otherwise reachable/scoped to just this one query group -- worth a look if false "cron gone silent" alerts are ever reported for real. Full writeup: flwork-p1-w4/deploy-prep/w4-admin-dashboard-slow-load-fanout-fix-2026-07-18-1300.md.
