cd /Users/jefftucker/flwork-p1-w2 first. You are WORKER W2 in the Full Loop CRM fleet. Read /Users/jefftucker/fullloopcrm/LEADER-HANDOFF.md in full for standing rules, then tail -80 /Users/jefftucker/fullloopcrm/LEADER-CHANNEL.md for recent context — several other sessions today already touched NYC Maid post-cutover bugs, check for overlap before duplicating work.

Hard rules:
- Work ONLY in this worktree (/Users/jefftucker/flwork-p1-w2), branch p1-w2. Never git stash/push --force/reset --hard.
- NEVER push to main, never run prod DB writes, never deploy — those are Jeff's three explicit gates. If a task seems to need one, stop and post to the channel asking, don't do it.
- After EVERY completed task: run real verification (tsc --noEmit, relevant vitest suite, actually exercise the fix) BEFORE reporting done. Then push this branch to origin (git push origin p1-w2) as an offsite backup — never main.
- Report to /Users/jefftucker/fullloopcrm/LEADER-CHANNEL.md by APPENDING lines in the exact format "HH:MM W2->LEADER: <message>".
- Never idle: when your queue is empty, re-read the channel for new orders before assuming you're done.

CRITICAL — all 3 of your items are NYC MAID SPECIFIC (the flagship tenant, cut over from its standalone build 2026-07-22). Per standing architecture rule: fix these gated to the nycmaid tenant only (.eq('tenant_id', <nycmaid's id>) or equivalent) — do NOT change behavior for the other ~21 tenants. If you find the root cause is actually shared/global code, STOP and post to the channel before changing shared logic — don't silently go global on a nycmaid-scoped ask.

Your queue:
1. Clients have been auto-deactivating unexpectedly for nycmaid. NOTE: a cron/lifecycle job was found mass-inactivating clients platform-wide and REMOVED from vercel.json yesterday (2026-07-22, commit b9e8a466f) — check first whether this is a recurrence (something re-added the cron, or a different code path doing the same thing) or genuinely new. Do not assume it's already fixed — verify against current prod cron config and recent client status changes.
2. Team profile pictures aren't showing, and team member data/star ratings aren't pulling correctly — investigate what data source they should read from and fix.
3. No check-in message is coming through in Telegram chat for nycmaid. Check /Users/jefftucker/fullloopcrm-nycmaid-telegram-checkin-2026-07-23 worktree first (branch investigate/nycmaid-telegram-checkin-2026-07-23) — someone may already be mid-investigation there today. If it's stale/abandoned, pick it up; if it's actively being worked, pick a different starting point and report the overlap to the channel.

Post an ack to the channel now with your plan for item 1, then start.
