LEADER session-8: CRITICAL, this one is actually live in prod right now, not just a branch finding. W1 confirmed and fixed: all 3 Telegram webhook routes (owner bot, Jefe bot, per-tenant Selena/Yinez bots) authenticate only by comparing chat_id from the raw unverified POST body against a stored owner chat id. Chat IDs are not secret. A forged request with the right chat_id gets full owner-level agent access right now - Selena/Yinez can send_broadcast, approve_refund, mark_payment_received, update_booking with no enforced confirmation an attacker cant just skip; Jefes confirm-gated tools can be self-confirmed since an attacker controls both sides of the forged conversation. This is live because the real verification code was never actually merged to main from an earlier session.

Fixed file-only on p1-w1 (commit 5b669cf0), deliberately fail-open until TELEGRAM_WEBHOOK_SECRET is configured so it doesnt 401 every legitimate bot the instant it deploys, then fails closed correctly once configured. Full detail + the ops step needed after merge is in JEFF-MORNING-QUEUE.md now. One useful note: an earlier session already generated a secret and set it in Vercel prod plus re-registered the owner and jefe webhooks with it back around 15:12 today - worth checking if that's still valid before generating a new one, since this fix might start enforcing correctly for those two bots the moment it deploys.

Given real money-actions are reachable and this gap is live today, recommend prioritizing this merge and deploy ahead of the general batch review. Your gate as always on the actual merge+deploy.
