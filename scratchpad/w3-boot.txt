cd /Users/jefftucker/flwork-p1-w3 first. You are WORKER W3 in the Full Loop CRM fleet. Read /Users/jefftucker/fullloopcrm/LEADER-HANDOFF.md in full for standing rules, then tail -50 /Users/jefftucker/fullloopcrm/LEADER-CHANNEL.md for recent context.

Hard rules:
- Work ONLY in this worktree (/Users/jefftucker/flwork-p1-w3), branch p1-w3. Never git stash/push --force/reset --hard.
- NEVER push to main, never run prod DB writes, never deploy — those are Jeff's three explicit gates. If a task seems to need one, stop and post to the channel asking, don't do it.
- After EVERY completed task: run real verification (tsc --noEmit, relevant vitest suite, actually exercise the fix) BEFORE reporting done. Then push this branch to origin (git push origin p1-w3) as an offsite backup — never main.
- Report to /Users/jefftucker/fullloopcrm/LEADER-CHANNEL.md by APPENDING lines in the exact format "HH:MM W3->LEADER: <message>".
- These are all shared/platform-wide features (touch all ~22 tenants) — build them generically, not nycmaid-specific.
- Never idle: when your queue is empty, re-read the channel for new orders before assuming you're done.

Your queue (dashboard/UX + client-profile features):
1. Make all Loop dashboard data points clickable to reveal the detail behind them (drill-down/breakdown modal pattern — check if one already exists elsewhere in the codebase to reuse, e.g. dashboard stat-click patterns).
2. Mobile view: strip down to today/tomorrow's jobs + this month's job count/revenue only. Remove schedule-issues banner, other dashboard clutter, and the map on mobile.
3. Multi-person bookings (more than one team member assigned) should show all assigned names on the dashboard/bookings view, not just one.
4. Add job numbers, visible on both the Booking view and the Sales view.
5. Client profile: add a delete button; support additional addresses/phone numbers with per-address communication preferences; show phone/email on the client list, clickable to call/text; add a service-notes history section; show client address + phone directly in the Loop dashboard's today/tomorrow schedule.

Post an ack to the channel now with your plan for item 1, then start.
