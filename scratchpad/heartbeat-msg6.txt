STATS 1619. W3's TOCTOU audit is now the big story: 6 real races found and fixed this session, not 4. New since last update: finding #5/HIGH -- bank-transactions match route had the same select-then-branch shape, two concurrent match requests on the same transaction (double-click, or two operators reconciling the same import) could both insert a payment, double-crediting revenue or double-marking a booking/invoice paid. Finding #6/HIGH is the biggest one -- journal-entry double-posting across the WHOLE ledger spine (payment revenue, deposits, refunds, chargebacks, commission accrual and payout, labor payroll -- 9 call sites total), root cause was a plain non-unique-index SELECT check backstopping every one of them, closed with a real UNIQUE index plus a shared atomic-insert guard. All 6 fixes have migration files prepared, none applied to prod. W4 audited the migrations for correctness (clean, matches the proven pattern) but found a real inherited defect: the atomic claim is never released if creation fails afterward, so a failed retry leaves the row permanently stuck needing a manual DB fix. Sent that back to W3 to close before this is truly done. Elsewhere: W1 nearly through its full 18-route O13 list (now on the last batch), W5 at 9 of 19 on its own separate list, W2 and W6 both fixing their own branch's PostgREST filter-injection gaps from W4's cross-branch table. All 6 lanes genuinely busy right now, no idle window yet for the channel-atomicity rollout. No new gated items. Gated queue: 2 DNS repoints, Part-0 pipeline, engine-cutover decision, test-tenant cleanup, plus now 5 real high-severity fixes (stripe + 4 of W3's TOCTOU set, the 6th pending W3's claim-release patch) ready for your merge/deploy review whenever you want them -- not pushing you to act, just naming the honest weight of what is sitting on branches.
