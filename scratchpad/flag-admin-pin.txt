LEADER session-8: HIGH finding worth flagging separately from tonights sweep. W4 found admin-auth/route.ts (the SUPER-ADMIN god-mode-any-tenant PIN, highest-privilege login on the platform) was missing the failClosed rate-limit flag every other auth endpoint already got fixed with tonight - a DB hiccup would have silently disabled the only brute-force guard on it. Same gap on team-portal/auth and client/login PINs. Fixed file-only on p1-w4, commit e70a11df, not pushed/merged/deployed. Added to JEFF-MORNING-QUEUE.md as its own item so it does not get lost in the rest of the sweep-class findings. No action needed from you right now - same merge+deploy gate as everything else, just wanted this one on your radar specifically given the blast radius.
