STATS 1722. Four more real security findings landed this tick, all fixed file-only, nothing applied to prod. (1) W1 closed a real OAuth login-CSRF gap (CWE-352) on Facebook/Instagram social-connect -- unlike Google's connect flow, these had no signed state param at all, so a forged callback could bind an attacker's social account to whatever tenant happened to be in the victim admin's active session. Ported the same signed-state pattern Google already uses, plus fixed a missing base-URL fallback that could leak the literal word undefined into a redirect URL. (2) W3 found the telnyx-voice webhook was checking header presence and timestamp freshness but never actually verifying the Ed25519 signature -- a real call-control auth bypass, now wired to the same verification its SMS sibling route already uses. (3) W4 found pin-reset has no rate limit on OTP guessing, unlike every sibling auth flow (portal/team-portal/referrers) which all throttle -- a 6-digit code with unlimited guesses is a real account-takeover path, now throttled the same way as its siblings. (4) W5 found the PostgREST search-injection gap (the one already fixed on one branch) in 5 more routes on its own branch, wired the existing sanitizer into all of them. Also: W2 landed a big sweep, 8 more real fixes across the null-clear and stuck-spinner bug classes on its branch in one pass. Fleet remains fully busy, no dedup issues, no new gated items. The gated queue is now substantial -- stripe money-race, 10+ TOCTOU instances across 6 branches, the verify-code constraint work, Telegram webhook auth, and now these 4 new security fixes, plus the original 4 items (DNS x2, Part-0 pipeline, engine-cutover, test-tenant cleanup). Not pushing for a review session, just keeping the count honest since it keeps growing.
