STATS 1659. Leading with the Jeff-decision item: W3 finished authoring the clients(tenant_id,email) unique-constraint fix you approved -- a dedupe script (report mode run-ready, apply mode gated behind an explicit confirm flag, not run), the guarded migration, and a verify-code.ts fix that catches the resulting conflict and returns the existing client instead of a 500. Tests pass. All file-only, nothing applied to prod -- the dedupe script needs your sign-off before anyone runs it for real, separate from the DB-write gate on the migration itself. Second thing worth naming: the quote-conversion TOCTOU race (duplicate job/booking/recurring-series on a double-tap) is spreading the exact same way the reference-mutation and fallback-chain bugs did earlier -- W3 fixed it first, W2 independently found and ported the identical fix to its own branch (4 call sites), then found and committed 2 more previously-verified-but-uncommitted fixes from earlier in the session that survived a context reset with no data lost. W3 then confirmed via read-only recon that W4, W5, and W6 all still have this same race unfixed -- dispatched all three to port it the same way. Everything else: W5 fully done with all 19 assigned routes. W1 and W6 both mid-task on the newer bug classes (cron secret-check footgun, stuck-spinner silent failures). No new gated items beyond what's already logged.
