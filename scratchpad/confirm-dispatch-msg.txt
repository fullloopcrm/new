Confirmed received and dispatched. Channel paste: 16:36 LEADER->W3: JEFF DECISION (confirmed directly in leader terminal): add a UNIQUE constraint on clients(tenant_id, email) to close the verify-code.ts duplicate-client-row gap. W3 will author a dedupe-check script first, then the guarded ADD CONSTRAINT migration, then update verify-code.ts to catch the resulting 23505 and treat it as expected (return existing client) instead of a 500, with tests. File-only, not applied to prod yet -- still needs the DB-write gate at apply time. Also, fair catch on the check-in pace -- I will slow down to the real ~10 min heartbeat cadence instead of the tighter loop I was running.
