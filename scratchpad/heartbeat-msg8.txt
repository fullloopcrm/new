STATS 1648. CRITICAL finding this tick, leading with it: W3's webhook/cron auth audit found all 3 Telegram webhooks (owner, jefe, per-tenant) have ZERO real origin verification in prod right now -- Telegram doesn't sign request bodies, and the existing "auth" just matched chat_id read from the same attacker-controlled POST body, so anyone who knows or guesses a chat_id can hit these endpoints as if they were the real bot. Fixed on p1-w3: real secret-token verification wired to all 3 routes, soft-gated so it will not break the live bots until you actually activate it (exact steps in deploy-prep/telegram-webhook-secret-activation.md, Jefe bot flagged highest priority). File-only, nothing deployed -- the real prod gap stays open until this merges, deploys, and you run the activation steps. Also flagged not fixed: telnyx-voice signature check fails OPEN if its public key env is unset, and ~30 cron routes do a bare bearer-compare without confirming CRON_SECRET is actually set first (a footgun, not currently exploitable under correct config). Elsewhere: W5 finished ALL 19 of its assigned routes, fully verified, now closing 2 more self-found bugs on its own branch. The notes:null-doesn't-clear bug class W5 first found is spreading -- W4 and W6 both found and fixed the same shape on their own branches, and W6 found a live variant in client-properties.ts (a stale unit number got baked back into an address after being cleared). W1's parallel sync-throw-leak recon (the one I mistakenly double-dispatched) hasn't reported yet -- W4 confirmed it did NOT execute the retracted duplicate, so no wasted work there despite my mistake. No new gated items beyond what is already logged.
