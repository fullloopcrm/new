// @ts-nocheck
import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Apply for a Home Services Job — Home Services Co",
  description: "Apply for a home services technician position. Competitive pay across HVAC, plumbing, electrical, painting, cleaning, handyman, and more. Licensed technicians with full benefits.",
  alternates: { canonical: "/apply" },
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
