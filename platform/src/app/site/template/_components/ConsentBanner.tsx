/**
 * Re-exports the shared consent banner (`src/components/consent/ConsentBanner.tsx`)
 * under the template's historical import path. New tenant integrations should
 * import from `@/components/consent/ConsentBanner` directly.
 */
export { default } from '@/components/consent/ConsentBanner'
