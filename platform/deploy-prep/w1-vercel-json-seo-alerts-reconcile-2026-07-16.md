# vercel.json / seo-alerts cross-branch reconciliation

**For:** whoever runs the merge/consolidation of the P1 worker branches.
**From:** W1, 17:25 order item (3).
**Status:** gap confirmed still present as of this writing (re-verified live against p1-w1, p1-w2, p1-w3, p1-w4, p1-w5, p1-w6 — no branch touched here).

**18:04 re-check (no new findings, situation unchanged):** re-diffed all 6 branches' `vercel.json` crons arrays and the gdpr-purge implementation files again. Nothing has moved since 17:37 — p1-w2 still has `seo-alerts`' code (route.ts/alerts.ts/alerts.test.ts, commit `2ebf48b2`) but no vercel.json entry for it; p1-w1 (this branch) still has only the entry; `seo-index-cliff` is still the mirror gap (w1 has both, w2/w3/w5/w6 have neither); w4's `seo-health` is still solo; `gdpr-purge` is still two live implementations at two schedules (w2 `src/lib/gdpr-deletion.ts` @ `0 9 * * *`, w5 `src/lib/gdpr.ts` @ `30 5 * * *`), still needing a pick-one decision, not a merge. No consolidation appears to have started yet per LEADER-CHANNEL. Nothing further to add until one of the branches actually moves or a merge attempt surfaces a new conflict.

**18:55 re-check (no new findings, situation unchanged):** re-verified live by grepping all 6 worktrees directly (not from memory): p1-w2 HEAD now at `81c41f58` (moved since 18:04, but the `seo-alerts` route.ts/alerts.ts are still present, unmerged, no vercel.json entry added on w2's side). p1-w1 HEAD now at `c1dcf2e0` — vercel.json entry still present, route.ts still absent here, matching the reverse `seo-index-cliff` gap (entry+route only on w1, both absent on w2/w3/w4/w5/w6). w4's `seo-health` still solo. gdpr-purge still exactly two competing implementations, unchanged (w2 `gdpr-deletion.ts`, w5 `gdpr.ts`). No merge/consolidation activity detected on any branch. Everything in this note as of 18:04 remains accurate verbatim; only the confirmation timestamp and w2's HEAD SHA have moved.

**20:1x re-check (no new findings, situation unchanged):** re-verified live against current HEADs — p1-w1 `457c39c9`, p1-w2 `53ecbb03`, p1-w3 `1c9de7e4`, p1-w4 `bd848f52`, p1-w5 `399cc29b`, p1-w6 `d956237a` (all moved since 18:55, expected — every worker has kept committing). Re-diffed vercel.json crons + the underlying files directly from each branch's tree (`git show <branch>:platform/vercel.json`, `git ls-tree`) rather than trusting memory: `seo-alerts` entry still w1-only, code still w2-only (`route.ts`, `alerts.ts` present on w2, absent on w1). `seo-index-cliff` mirror gap unchanged — entry+code both present only on w1, absent on w2/w3/w4/w5/w6. `seo-health` still solo on w4. `gdpr-purge` still two independently-built implementations at two schedules, unchanged (w2 `gdpr-deletion.ts` @ `0 9 * * *`, w5 `gdpr.ts` @ `30 5 * * *`). p1-w3 and p1-w6 have neither seo-alerts/seo-index-cliff/seo-health/gdpr-purge code nor entries — consistent with prior checks. No consolidation/merge activity detected on any branch per current LEADER-CHANNEL contents. Nothing in this note has changed since 18:55 except branch HEAD SHAs and the confirmation timestamp.

## The gap

`platform/vercel.json` on **p1-w1** has a crons entry:

```json
{ "path": "/api/cron/seo-alerts", "schedule": "15 8 * * 2" }
```

I (W1) added this entry at 05:18 alongside `seo-index-cliff`, per LEADER's 05:12 order to wire both into vercel.json ahead of the eventual consolidated push. But the route it points at, and everything that route needs, was built on a **different** branch:

- `platform/src/app/api/cron/seo-alerts/route.ts` — **only on p1-w2**, commit `2ebf48b2`
- `platform/src/lib/seo/alerts.ts` (`checkCriticalSeoAlerts`, the actual logic) — **only on p1-w2**, same commit
- `platform/src/lib/seo/alerts.test.ts` — **only on p1-w2**, same commit
- `platform/src/lib/migrations/2026_07_16_seo_alert_snapshots.sql` (new `seo_alert_snapshots` dedup-state table `alerts.ts` reads/writes) — **only on p1-w2**, same commit, **not applied anywhere**

p1-w1 has the vercel.json line but none of the code or schema behind it. If p1-w1 (or any branch missing these 4 files) is pushed as-is with this vercel.json entry live, the cron will fire `GET /api/cron/seo-alerts` on a schedule and 404 — not a build break, but a silent no-op alert path that looks configured and isn't.

## What it takes to close it

1. Bring the 3 code/test files from p1-w2's `2ebf48b2` onto the merge base (`git show p1-w2:platform/src/app/api/cron/seo-alerts/route.ts` etc., or a normal merge/cherry-pick if p1-w2 lands in the same merge).
2. Apply the `2026_07_16_seo_alert_snapshots.sql` migration (new table, additive, low risk) — needs Jeff's approval + the leader to run it, same as every other migration in this sweep. Without it, `alerts.ts` will error on every cron tick once the route exists (it reads/writes `seo_alert_snapshots` for the dedup fingerprint).
3. Re-run `tsc --noEmit` + `alerts.test.ts` once the files coexist with p1-w1's other seo-* additions (`seo-index-cliff`, `technical.ts`'s sitemap repopulation the alerts cron runs after) — W2's tests only ever ran against p1-w2's tree, not against p1-w1's, so this exact combination hasn't been verified together yet.

## This is not actually a one-directional gap — the reverse also exists

The same vercel.json diff shows `seo-index-cliff` is the **mirror image** of this problem: p1-w1 has both the entry *and* the code (`src/app/api/cron/seo-index-cliff/route.ts`, confirmed present); p1-w2 (and w3, w5, w6) has **neither** the entry nor the code. So a plain union-merge of vercel.json isn't enough — whoever consolidates needs both directions: pull `seo-alerts`'s code from w2 onto the base, and confirm `seo-index-cliff`'s code (already on w1) survives the merge too.

## Adjacent same-file hazard found while checking this (flagging, not fixing)

`vercel.json`'s crons array has diverged in more ways than just the SEO entries across the branches I diffed against p1-w1:

| Entry | Present on | Notes |
|---|---|---|
| `generate-monthly-invoices` (`0 7 1 * *`) | w1 only | W1's own consolidated-invoicing feature (commit `3017117b`); every other branch lacks it — needs merging in regardless of the SEO question. |
| `seo-health` (`0 9 * * *`) | w4 only | Route confirmed present on p1-w4 (`c28b4a36`), absent on w1/w2/w3/w5/w6. |
| `gdpr-purge` | **w2 at `0 9 * * *`** vs **w5 at `30 5 * * *`** | Worse than a missing-code gap: **two different branches independently built two different GDPR-deletion implementations** (w2: `src/lib/gdpr-deletion.ts`, commit `94cef8ed`; w5: `src/lib/gdpr.ts`, per earlier channel note) and each wired its own cron at the *same path* with a *different schedule*. This can't be merged mechanically — whoever consolidates has to pick one implementation, and the losing branch's route file + migration become dead code to drop, not code to merge in. |

I only diffed w1 against w2/w3/w4/w5/w6 (all 6 P1 worktrees that exist as of now) — a real 3+ way merge of this one file needs whoever does it to treat vercel.json as requiring manual reconciliation, not a mechanical union of all branches' diffs.

**21:29 re-check (no new findings, situation unchanged):** re-verified live against current HEADs — p1-w1 `d8cf9732`, p1-w2 `f485ff15`, p1-w3 `602b5a13`, p1-w4 `db5e1c8d`, p1-w5 `399cc29b`, p1-w6 `d956237a` (all moved since the 20:1x check, expected). Re-diffed vercel.json crons directly from each branch's tree: w1 still carries both `seo-alerts` and `seo-index-cliff` entries plus its solo `generate-monthly-invoices` entry; code-side w1 still has only `seo-index-cliff`'s route.ts, `seo-alerts`' route.ts still lives only on w2 (with no vercel.json entry there). `seo-health` still solo (entry+code) on w4. `gdpr-purge` still two independently-built implementations at their original schedules, unchanged (w2 `gdpr-deletion.ts` @ `0 9 * * *`, w5 `gdpr.ts` @ `30 5 * * *`). w3/w6 still have neither entries nor code for any of these. No consolidation/merge activity detected on any branch per current LEADER-CHANNEL contents. Nothing in this note has changed since the last check except branch HEAD SHAs and the confirmation timestamp.

**21:38 re-check — NEW finding, w4 added a 6th solo cron entry:** re-verified live against current HEADs — p1-w1 `10cc3c1a`, p1-w2 `f485ff15`, p1-w3 `529decb2`, p1-w4 `26c7de77`, p1-w5 `399cc29b`, p1-w6 `d956237a`. Everything from the 21:29 check is still true (seo-alerts entry-only on w1/code-only on w2, seo-index-cliff entry+code solo on w1, seo-health entry+code solo on w4, gdpr-purge two competing implementations on w2/w5, w3/w6 baseline). New since 21:29: **w4 added `hr-document-reminders`** (`platform/src/app/api/cron/hr-document-reminders/route.ts` + `route.duplicate-send.test.ts`, commit `277e9056`, "feat(hr): add daily hr_document_reminders expiry nudge cron") with its own vercel.json crons entry — same shape of gap as `seo-health`: entry+code present on w4 only, absent on w1/w2/w3/w5/w6. Unlike `seo-alerts`, no migration gap here: the `hr_document_reminders` table the route writes to is defined in `053_hr_foundation.sql`, which is already present on w1 (dated Jul 11, predates this branch split) — so closing this one is just pulling the route + test onto the merge base and adding the vercel.json entry, no schema step required. No consolidation/merge activity detected on any branch per current LEADER-CHANNEL contents.

**21:56 re-check (no new findings, situation unchanged):** re-verified live against current HEADs — p1-w1 `a7ddbe2a`, p1-w2 `bacb1f72`, p1-w3 `397ff788`, p1-w4 `aae7ca88`, p1-w5 `399cc29b`, p1-w6 `d956237a` (w5/w6 haven't moved since Jul 13, still stale). Re-diffed all 6 `vercel.json` crons arrays directly: everything from the 21:38 check is unchanged — `seo-alerts` entry-only on w1/code-only on w2, `seo-index-cliff` entry+code solo on w1, `seo-health` entry+code solo on w4, `hr-document-reminders` entry+code solo on w4, `gdpr-purge` still two competing implementations on w2 (`0 9 * * *`) and w5 (`30 5 * * *`), w3/w6 still baseline (neither entries nor code for any of these). No consolidation/merge activity detected on any branch per current LEADER-CHANNEL contents.

**22:35 re-check (no new findings, situation unchanged):** re-verified live against current HEADs — p1-w1 `0d2c1e9e`, p1-w2 `d63458dd`, p1-w3 `6ee012f4`, p1-w4 `41a39d8a`, p1-w5 `533410e8`, p1-w6 `f30fff76` (w5/w6 both moved off their Jul-13 SHAs since the 21:56 check). Re-diffed all 6 `vercel.json` crons arrays directly plus re-confirmed the underlying files: `seo-alerts` still entry-only on w1 (`seo-alerts/route.ts` absent), code-only on w2 (route.ts present, no vercel.json entry); `seo-index-cliff` still entry+code solo on w1; `seo-health` and `hr-document-reminders` still entry+code solo on w4 (both absent on w1); `gdpr-purge` still two independently-built implementations, unchanged schedules (w2 `gdpr-deletion.ts` @ `0 9 * * *`, w5 `gdpr.ts` @ `30 5 * * *`, both files still present on their respective branches). w5/w6 moving didn't touch any of these files. No consolidation/merge activity detected on any branch per current LEADER-CHANNEL contents.

**23:37 re-check (no new committed-tree findings; w5/w6 confirmed stale, w1 has new uncommitted noise):** re-verified live against current HEADs — p1-w1 `e971e392`, p1-w2 `14d0ecbf`, p1-w3 `b568cade`, p1-w4 `c4880462`, p1-w5 `399cc29b` (unchanged, Jul-13 18:23 — still stale), p1-w6 `d956237a` (unchanged, Jul-13 18:56 — still stale). Re-diffed all 6 `vercel.json` crons arrays via `git show <branch>:platform/vercel.json` plus `git cat-file -e <branch>:<path>` for every gap file (committed-tree only, not working-directory state): everything from the 22:35 check is unchanged — `seo-alerts` entry-only on w1/code-only on w2, `seo-index-cliff` entry+code solo on w1, `seo-health` + `hr-document-reminders` entry+code solo on w4, `gdpr-purge` still two competing implementations at their original schedules (w2 `gdpr-deletion.ts` @ `0 9 * * *`, w5 `gdpr.ts` @ `30 5 * * *`), w3/w6 still baseline. w5/w6's SHAs match the 21:56-and-earlier stale reading exactly — they have not moved since Jul 13 across this entire reconciliation window. One new wrinkle checked and ruled out: this worktree's `git status` currently shows untracked `platform/src/app/api/cron/seo-health/` and `seo-improve/` directories sitting in the working tree. Verified via `git cat-file -e p1-w1:platform/src/app/api/cron/seo-health/route.ts` that this is **not** a new committed gap — it's uncommitted working-tree content only (the same untracked leftover the 20:31/20:42 exchange already investigated and correctly declined to delete, since it traces to real committed+deployed SEOMGR history elsewhere, not this worktree's git history). Doesn't change any row in the table above. No consolidation/merge activity detected on any branch per current LEADER-CHANNEL contents.

## Re-verify 23:56

Unchanged since 23:35 (bef21b8b): w1 solo on seo-alerts+seo-index-cliff, w2+w5 share gdpr-purge, w4 solo on seo-health+hr-document-reminders, w3/w6 stale. No consolidation activity across any branch.

## Re-verify 00:12 (2026-07-17)

Re-verified live against current HEADs — p1-w1 `633f3bf2`, p1-w2 `1e1d2716`, p1-w3 `89955b55`, p1-w4 `7236556f`, p1-w5 `399cc29b` (unchanged, still Jul-13 18:23 — stale), p1-w6 `d956237a` (unchanged, still Jul-13 18:56 — stale). Re-diffed all 6 `vercel.json` crons arrays via `git show <branch>:platform/vercel.json`: everything from the 23:56 check is unchanged for the tracked gap items — `seo-alerts` still entry-only on w1/code-only on w2, `seo-index-cliff` still entry+code solo on w1, `seo-health` + `hr-document-reminders` still entry+code solo on w4, `gdpr-purge` still two competing implementations at their original schedules (w2 `0 9 * * *`, w5 `30 5 * * *`), w3/w6 still baseline. One addition on w1 since the last full listing, not a gap: `generate-monthly-invoices` (this session's own monthly-invoice-consolidation feature, first committed ~23:xx) is now a third w1-solo cron entry, alongside seo-alerts/seo-index-cliff — expected, self-consistent with the code it ships (`src/app/api/cron/generate-monthly-invoices/route.ts` exists only on w1), not cross-branch drift. No consolidation/merge activity detected on any branch per current LEADER-CHANNEL contents.

## Re-verify 00:26 (2026-07-17)

Re-verified live against current HEADs — p1-w1 `0e0aac94`, p1-w2 `573890c5`, p1-w3 `25c79cfd`, p1-w4 `ba036037`, p1-w5 `399cc29b` (unchanged, still Jul-13 18:23 — stale), p1-w6 `d956237a` (unchanged, still Jul-13 18:56 — stale). Re-diffed all 6 `vercel.json` crons arrays via `git show <branch>:platform/vercel.json`: unchanged from the 00:12 check — `seo-alerts` entry-only on w1/code-only on w2, `seo-index-cliff` entry+code solo on w1, `seo-health` + `hr-document-reminders` entry+code solo on w4, `gdpr-purge` two competing implementations at their original schedules (w2 `0 9 * * *`, w5 `30 5 * * *`), w3/w6 still baseline, w1's `generate-monthly-invoices` still its own solo entry (unchanged, not new drift). No consolidation/merge activity detected on any branch per current LEADER-CHANNEL contents.

## Re-verify 00:38 (2026-07-17)

Re-verified live against current HEADs — p1-w1 `92cabca4` (this session's own commit, the BookingsAdmin.tsx recurring_type display-string-vs-schedule-writer fix — doesn't touch vercel.json or any file in this gap's scope), p1-w2 `b319d3cb`, p1-w3 `c863f61a`, p1-w4 `0198a7f7`, p1-w5 `399cc29b` (unchanged, still Jul-13 18:23 — stale), p1-w6 `d956237a` (unchanged, still Jul-13 18:56 — stale). Re-diffed all 6 `vercel.json` crons arrays via `git show <branch>:platform/vercel.json` and re-confirmed every gap file via `git cat-file -e`: unchanged from the 00:26 check across every row — `seo-alerts` entry-only on w1/code-only on w2, `seo-index-cliff` entry+code solo on w1, `seo-health` + `hr-document-reminders` entry+code solo on w4, `gdpr-purge` two competing implementations at their original schedules (w2 `gdpr-deletion.ts` @ `0 9 * * *`, w5 `gdpr.ts` @ `30 5 * * *`), w3/w6 still baseline, w1's `generate-monthly-invoices` still its own solo entry. No consolidation/merge activity detected on any branch per current LEADER-CHANNEL contents.

## Re-verify 01:00 (2026-07-17)

Re-verified live against current HEADs — p1-w1 `f2d15a9a` (this session's own commit, the `monthly_weekday` month-boundary fix in `recurring.ts` — doesn't touch vercel.json or any file in this gap's scope), p1-w2 `b319d3cb`, p1-w3 `9ac34fbd`, p1-w4 `8f397152`, p1-w5 `399cc29b` (unchanged, still Jul-13 18:23 — stale), p1-w6 `d956237a` (unchanged, still Jul-13 18:56 — stale). Re-diffed all 6 `vercel.json` crons arrays via `git show <branch>:platform/vercel.json` and re-confirmed every gap file via `git cat-file -e`: unchanged from the 00:38 check across every row — `seo-alerts` entry-only on w1/code-only on w2, `seo-index-cliff` entry+code solo on w1, `seo-health` + `hr-document-reminders` entry+code solo on w4, `gdpr-purge` still two competing implementations at their original schedules (w2 `gdpr-deletion.ts` @ `0 9 * * *`, w5 `gdpr.ts` @ `30 5 * * *`), w3/w6 still baseline, w1's `generate-monthly-invoices` still its own solo entry. No consolidation/merge activity detected on any branch per current LEADER-CHANNEL contents.

## Re-verify 01:13 (2026-07-17)

Re-verified live against current HEADs — p1-w1 `3ce316e9` (this session's own commit, the naive-ET vs UTC `start_time` "now" comparison fix across recurring-schedules/schedules/telnyx/system-check — doesn't touch vercel.json or any file in this gap's scope), p1-w2 `077d3362`, p1-w3 `36ec9f3b`, p1-w4 `e33f55ef`, p1-w5 `399cc29b` (unchanged, still Jul-13 18:23 — stale), p1-w6 `d956237a` (unchanged, still Jul-13 18:56 — stale). Re-diffed all 6 `vercel.json` crons arrays via `git show <branch>:platform/vercel.json` and re-confirmed every gap file via `git cat-file -e`: unchanged from the 01:00 check across every row — `seo-alerts` entry-only on w1/code-only on w2, `seo-index-cliff` entry+code solo on w1, `seo-health` + `hr-document-reminders` entry+code solo on w4, `gdpr-purge` still two competing implementations at their original schedules (w2 `gdpr-deletion.ts` @ `0 9 * * *`, w5 `gdpr.ts` @ `30 5 * * *`), w3/w6 still baseline, w1's `generate-monthly-invoices` still its own solo entry. No consolidation/merge activity detected on any branch per current LEADER-CHANNEL contents.

## Re-verify 01:41 (2026-07-17)

Re-verified live against current HEADs — p1-w1 `4b99fe21` (this session's own commit, the BookingsAdmin.tsx `monthly_day` anchor-drop/no-fallback fix in `_recurring.ts` — doesn't touch vercel.json or any file in this gap's scope), p1-w2 `d139dda8`, p1-w3 `5b9cc0c6`, p1-w4 `c12660be`, p1-w5 `399cc29b` (unchanged, still Jul-13 18:23 — stale), p1-w6 `d956237a` (unchanged, still Jul-13 18:56 — stale). Re-diffed all 6 `vercel.json` crons arrays via `git show <branch>:platform/vercel.json` and re-confirmed every gap file via `git cat-file -e`: unchanged from the 01:13 check across every row — `seo-alerts` entry-only on w1/code-only on w2, `seo-index-cliff` entry+code solo on w1, `seo-health` + `hr-document-reminders` entry+code solo on w4, `gdpr-purge` still two competing implementations at their original schedules (w2 `gdpr-deletion.ts` @ `0 9 * * *`, w5 `gdpr.ts` @ `30 5 * * *`), w3/w6 still baseline, w1's `generate-monthly-invoices` still its own solo entry. No consolidation/merge activity detected on any branch per current LEADER-CHANNEL contents.

## Re-verify 01:52 (2026-07-17)

Re-verified live against current HEADs — p1-w1 `64187c28` (this session's own commit, the `monthly_date` permanent-drift fix + `monthly_weekday` day-29-31 order-of-operations fix across `lib/recurring.ts` and `dashboard/bookings/_recurring.ts` — doesn't touch vercel.json or any file in this gap's scope), p1-w2 `779b16b2`, p1-w3 `c01d5b9f`, p1-w4 `3391dde6`, p1-w5 `399cc29b` (unchanged, still Jul-13 18:23 — stale), p1-w6 `d956237a` (unchanged, still Jul-13 18:56 — stale). Re-diffed all 6 `vercel.json` crons arrays via `git show <branch>:platform/vercel.json` and re-confirmed every gap file via `git cat-file -e`: unchanged from the 01:41 check across every row — `seo-alerts` entry-only on w1/code-only on w2, `seo-index-cliff` entry+code solo on w1, `seo-health` + `hr-document-reminders` entry+code solo on w4, `gdpr-purge` still two competing implementations at their original schedules (w2 `gdpr-deletion.ts` @ `0 9 * * *`, w5 `gdpr.ts` @ `30 5 * * *`), w3/w6 still baseline, w1's `generate-monthly-invoices` still its own solo entry. No consolidation/merge activity detected on any branch per current LEADER-CHANNEL contents.

## Re-verify 01:56 (2026-07-17)

Re-verified live against current HEADs — p1-w1 `91919561` (this session's own commit, the recurring-expenses cron's monthly/quarterly `advance()` permanent-drift fix — same bug class as `lib/recurring.ts`'s, but in `src/app/api/cron/recurring-expenses/route.ts`; doesn't touch vercel.json or any file in this gap's scope, `recurring-expenses`'s own cron entry is already present+identical on all 6 branches, not part of this gap), p1-w2 `014f6a32`, p1-w3 `eae82eee`, p1-w4 `5f6c9954`, p1-w5 `399cc29b` (unchanged, still Jul-13 18:23 — stale), p1-w6 `d956237a` (unchanged, still Jul-13 18:56 — stale). Re-diffed all 6 `vercel.json` crons arrays via `git show <branch>:platform/vercel.json`: unchanged from the 01:52 check across every row — `seo-alerts` entry-only on w1/code-only on w2, `seo-index-cliff` entry+code solo on w1, `seo-health` + `hr-document-reminders` entry+code solo on w4, `gdpr-purge` still two competing implementations at their original schedules (w2 `gdpr-deletion.ts` @ `0 9 * * *`, w5 `gdpr.ts` @ `30 5 * * *`), w3/w6 still baseline, w1's `generate-monthly-invoices` still its own solo entry. No consolidation/merge activity detected on any branch per current LEADER-CHANNEL contents.

## Re-verify 02:10 (2026-07-17)

Re-verified live against current HEADs — p1-w1 `f44356dc` (this session's own commits: cash-flow forecast `advanceCursor()` permanent-drift fix + Selena `lookup_bookings` naive-ET fix — neither touches vercel.json or any file in this gap's scope), p1-w2 `273b121f`, p1-w3 `c856e9d9`, p1-w4 `32278d31`, p1-w5 `399cc29b` (unchanged, still Jul-13 18:23 — stale), p1-w6 `d956237a` (unchanged, still Jul-13 18:56 — stale). Re-diffed all 6 `vercel.json` crons arrays via `git show <branch>:platform/vercel.json` and re-confirmed every gap file via `git cat-file -e`/`git ls-tree`: unchanged from the 01:56 check across every row — `seo-alerts` entry-only on w1/code-only on w2 (`seo-alerts/route.ts` present on w2, absent on w1), `seo-index-cliff` entry+code solo on w1, `seo-health` + `hr-document-reminders` entry+code solo on w4 (both absent on w1's committed tree), `gdpr-purge` still two independently-built implementations at their original schedules (w2 `src/lib/gdpr-deletion.ts` @ `0 9 * * *`, w5 `src/lib/gdpr.ts` @ `30 5 * * *`, both branches also carry their own `src/app/api/cron/gdpr-purge/route.ts`), w3/w6 still baseline, w1's `generate-monthly-invoices` still its own solo entry (unchanged, not new drift). No consolidation/merge activity detected on any branch per current LEADER-CHANNEL contents.

## Re-verify 02:21 (2026-07-17)

Re-verified live against current HEADs — p1-w1 `c76eae55` (this session's own commit: finance/revenue + admin/finance's 12-month trend-chart `setMonth()` overflow fix, deduplicated into `src/lib/finance/trailing-month-keys.ts` — doesn't touch vercel.json or any file in this gap's scope), p1-w2 `e10fc02f`, p1-w3 `4bf26af5`, p1-w4 `ec5820b8`, p1-w5 `399cc29b` (unchanged, still Jul-13 18:23 — stale), p1-w6 `d956237a` (unchanged, still Jul-13 18:56 — stale). Re-diffed all 6 `vercel.json` crons arrays via `git show <branch>:platform/vercel.json`: unchanged from the 02:10 check across every row — `seo-alerts` entry-only on w1/code-only on w2, `seo-index-cliff` entry+code solo on w1, `seo-health` + `hr-document-reminders` entry+code solo on w4, `gdpr-purge` still two independently-built implementations at their original schedules (w2 `0 9 * * *`, w5 `30 5 * * *`), w3/w6 still baseline, w1's `generate-monthly-invoices` still its own solo entry. No consolidation/merge activity detected on any branch per current LEADER-CHANNEL contents.

## Re-verify 02:29 (2026-07-17) — NEW row added, not previously tracked

Re-verified live against current HEADs — p1-w1 `39c45d67` (this session's own commit: platform-admin `analytics/page.tsx` signups-by-month `setMonth()` overflow fix, extending `trailing-month-keys.ts` with `buildTrailingMonths()` — doesn't touch vercel.json or any file in this gap's scope), p1-w2 `7fbd3115`, p1-w3 `49afeb1c`, p1-w4 `ec5820b8` (unchanged since 02:21), p1-w5 `399cc29b` (unchanged, still Jul-13 18:23 — stale), p1-w6 `d956237a` (unchanged, still Jul-13 18:56 — stale). Re-diffed all 6 `vercel.json` crons arrays via `git show <branch>:platform/vercel.json` and re-confirmed every gap file via `git cat-file -e`: every previously-tracked row unchanged — `seo-alerts` entry-only on w1/code-only on w2, `seo-index-cliff` entry+code solo on w1, `seo-health` + `hr-document-reminders` entry+code solo on w4, `gdpr-purge` still two independently-built implementations at their original schedules (w2 `0 9 * * *`, w5 `30 5 * * *`), w3/w6 still baseline, w1's `generate-monthly-invoices` still its own solo entry.

**New divergence found this pass, not previously in this doc's table:** `refresh-job-postings`'s **schedule** (not presence — the route exists on all 6 branches, code-identical apart from a comment) differs: p1-w1 runs `0 3 1,16 * *` (1st/16th of month, ~2x/month), every other branch (`w2`/`w3`/`w4`/`w5`/`w6`) still runs `0 3 * * *` (daily). This is a deliberate w1-only change (commit `457c39c9`, "Vercel cost -- refresh-job-postings cron did daily full-layout ISR invalidation... when only 15-day freshness is required") — cuts the cron's ISR-invalidation fan-out ~15x with no functional/content difference (route.ts diff is schedule-comment-only, no logic branch on day-of-month). Not a missing-code gap like the others in this table — whoever consolidates needs to carry w1's cheaper schedule forward rather than mechanically resolve the vercel.json merge conflict toward the more common (`0 3 * * *`) value across the other 5 branches, or the Vercel cost fix silently reverts on merge.

## Re-verify 04:20 (2026-07-17)

Re-verified live against current HEADs — p1-w1 `b4ec19a7` (this session's own commit: team-member quiet-hours push suppression's `isQuietHours()` server-UTC-hour-vs-ET-hour fix in `notify-team-member.ts` + `notify-team.ts`, plus a new `etMinuteOfDay()` helper in `recurring.ts` — neither touches vercel.json or any file in this gap's scope), p1-w2 `a96be57e`, p1-w3 `ee37960f`, p1-w4 `4e517595`, p1-w5 `399cc29b` (unchanged, still Jul-13 18:23 — stale), p1-w6 `d956237a` (unchanged, still Jul-13 18:56 — stale). Re-diffed all 6 `vercel.json` crons arrays via `git show <branch>:platform/vercel.json` (full array dump, not just the tracked paths) and re-confirmed every gap file via `git cat-file -e`: unchanged from the 04:11 check across every row — `seo-alerts` entry-only on w1/code-only on w2, `seo-index-cliff` entry+code solo on w1, `seo-health` + `hr-document-reminders` entry+code solo on w4, `gdpr-purge` still two independently-built implementations at their original schedules (w2 `gdpr-deletion.ts` @ `0 9 * * *`, w5 `gdpr.ts` @ `30 5 * * *`), w3/w6 still baseline, w1's `generate-monthly-invoices` + `refresh-job-postings`'s cheaper 1st/16th schedule both still w1-solo. No consolidation/merge activity detected on any branch per current LEADER-CHANNEL contents.

## Re-verify 02:57 (2026-07-17)

Re-verified live against current HEADs — p1-w1 `84846a1f` (this session's own commits: 9-file `nowNaiveET()` sweep for the same true-UTC-vs-naive-ET `start_time`/`end_time` cutoff bug class, plus a new `parseNaiveET()` helper + 15min-alert billing-hours fix — neither touches vercel.json or any file in this gap's scope), p1-w2 `16850410`, p1-w3 `76a5c881`, p1-w4 `0d8d9976`, p1-w5 `399cc29b` (unchanged, still Jul-13 18:23 — stale), p1-w6 `d956237a` (unchanged, still Jul-13 18:56 — stale). Re-diffed all 6 `vercel.json` crons arrays via `git show <branch>:platform/vercel.json` and re-confirmed every gap file via `git cat-file -e`: unchanged from the 02:29 check across every row — `seo-alerts` entry-only on w1/code-only on w2, `seo-index-cliff` entry+code solo on w1, `seo-health` + `hr-document-reminders` entry+code solo on w4, `gdpr-purge` still two independently-built implementations at their original schedules (w2 `0 9 * * *`, w5 `30 5 * * *`), w3/w6 still baseline, w1's `generate-monthly-invoices` + `refresh-job-postings`'s cheaper 1st/16th schedule both still w1-solo (unchanged, not new drift). No consolidation/merge activity detected on any branch per current LEADER-CHANNEL contents.

## Re-verify 03:18 (2026-07-17)

Re-verified live against current HEADs — p1-w1 `975d7db8` (this session's own commits: day-boundary counterpart of the naive-ET/UTC sweep in bookings/stats + team-portal/earnings, then the same bug class in the main dashboard aggregator — neither touches vercel.json or any file in this gap's scope), p1-w2 `71e1c66e`, p1-w3 `268042a5`, p1-w4 `f10de5f5`, p1-w5 `399cc29b` (unchanged, still Jul-13 18:23 — stale), p1-w6 `d956237a` (unchanged, still Jul-13 18:56 — stale). Re-diffed all 6 `vercel.json` crons arrays via `git show <branch>:platform/vercel.json` and re-confirmed every gap file via `git cat-file -e`: unchanged from the 02:57 check across every row — `seo-alerts` entry-only on w1/code-only on w2, `seo-index-cliff` entry+code solo on w1, `seo-health` + `hr-document-reminders` entry+code solo on w4, `gdpr-purge` still two independently-built implementations at their original schedules (w2 `gdpr-deletion.ts` @ `0 9 * * *`, w5 `gdpr.ts` @ `30 5 * * *`), w3/w6 still baseline, w1's `generate-monthly-invoices` + `refresh-job-postings`'s cheaper 1st/16th schedule both still w1-solo. No consolidation/merge activity detected on any branch per current LEADER-CHANNEL contents.

## Re-verify 03:45 (2026-07-17)

Re-verified live against current HEADs — p1-w1 `9cac471f` (this session's own commits: day-summary/late-check-in day-boundary sweep, then cron/confirmations' `now.getHours()` ET-hour-gate bug — the fresh-ground find this round, a genuinely different mechanism than the day-boundary bug class; neither touches vercel.json or any file in this gap's scope), p1-w2 `13bfb184`, p1-w3 `d9e999dc`, p1-w4 `3883c2d7`, p1-w5 `399cc29b` (unchanged, still Jul-13 18:23 — stale), p1-w6 `d956237a` (unchanged, still Jul-13 18:56 — stale). Re-diffed all 6 `vercel.json` crons arrays via `git show <branch>:platform/vercel.json` (git-tracked state only) and re-confirmed every gap file via `git ls-tree`: unchanged from the 03:18 check across every row — `seo-alerts` entry-only on w1/code-only on w2, `seo-index-cliff` entry+code solo on w1, `seo-health` + `hr-document-reminders` entry+code solo on w4, `gdpr-purge` still two independently-built implementations at their original schedules (w2 `0 9 * * *`, w5 `30 5 * * *`), w3/w6 still baseline, w1's `generate-monthly-invoices` + `refresh-job-postings`'s cheaper 1st/16th schedule both still w1-solo. Re-confirmed the untracked `seo-health`/`seo-improve` working-tree noise flagged at 23:37 is still just uncommitted leftover (not a new committed-tree gap) via `git status` + `git ls-tree HEAD` mismatch — same conclusion as before, nothing to act on. No consolidation/merge activity detected on any branch per current LEADER-CHANNEL contents.

## Re-verify 04:11 (2026-07-17)

Re-verified live against current HEADs — p1-w1 `792120dd` (this session's own commits: reminders cron's 6 remaining `now.getHours()===N` ET-hour gates closed, then a fresh-ground find in schedule/calendar's month-grid boundary + now-vs-naive-ET comparisons — neither touches vercel.json or any file in this gap's scope), p1-w2 `a96be57e`, p1-w3 `ee37960f`, p1-w4 `1d2e8009`, p1-w5 `399cc29b` (unchanged, still Jul-13 18:23 — stale), p1-w6 `d956237a` (unchanged, still Jul-13 18:56 — stale). Re-diffed all 6 `vercel.json` crons arrays via `git show <branch>:platform/vercel.json` and re-confirmed every gap file via `git cat-file -e`: unchanged from the 03:45 check across every row — `seo-alerts` entry-only on w1/code-only on w2, `seo-index-cliff` entry+code solo on w1, `seo-health` + `hr-document-reminders` entry+code solo on w4, `gdpr-purge` still two independently-built implementations at their original schedules (w2 `gdpr-deletion.ts` @ `0 9 * * *`, w5 `gdpr.ts` @ `30 5 * * *`), w3/w6 still baseline, w1's `generate-monthly-invoices` + `refresh-job-postings`'s cheaper 1st/16th schedule both still w1-solo. No consolidation/merge activity detected on any branch per current LEADER-CHANNEL contents.

## Re-verify 04:35 (2026-07-17)

Re-verified live against current HEADs — p1-w1 `37793164` (this session's own commits: recurring-schedules regenerate route's `from_date`-omitted cutoff naive-ET/UTC fix, then a fresh-ground find in the no-show-check + confirmation-reminder crons — same bug class, neither touches vercel.json or any file in this gap's scope), p1-w2 `a2269b8b` (moved since 04:20, docs-only sms_consent gap-doc commit, doesn't touch vercel.json), p1-w3 `3f993573` (moved since 04:20, docs-only gap-doc commit, doesn't touch vercel.json), p1-w4 `393c5ca6` (moved since 04:20, docs-only gap-doc commit), p1-w5 `399cc29b` (unchanged, still Jul-13 18:23 — stale), p1-w6 `d956237a` (unchanged, still Jul-13 18:56 — stale). Re-diffed all 6 `vercel.json` crons arrays via `git show <branch>:platform/vercel.json` (full array dump, hashed for a quick equality check, then read in full): unchanged from the 04:20 check across every row — `seo-alerts` entry-only on w1/code-only on w2, `seo-index-cliff` entry+code solo on w1, `seo-health` + `hr-document-reminders` entry+code solo on w4, `gdpr-purge` still two independently-built implementations at their original schedules (w2 `gdpr-deletion.ts` @ `0 9 * * *`, w5 `gdpr.ts` @ `30 5 * * *`), w3/w6 still baseline, w1's `generate-monthly-invoices` + `refresh-job-postings`'s cheaper 1st/16th schedule both still w1-solo. No consolidation/merge activity detected on any branch per current LEADER-CHANNEL contents.

## Re-verify 04:53 (2026-07-17)

Re-verified live against current HEADs — p1-w1 `f60c8373` (this session's own commits since 04:35: LEADER's fresh queue item — reviewed the 5 flagged naive-ET/true-UTC call sites, landed fixes in team-portal/jobs, team-portal/jobs/claim, and cron/outreach (same bug class as tonight's other fixes) plus a deeper ET-calendar-boundary fix in finance/summary; client/bookings confirmed already correct — none touch vercel.json or any file in this gap's scope), p1-w2 `8fe8d4e9` (moved since 04:35: `lastNMonths()` anchor-overflow fix + regression tests, a different bug class (setMonth/setUTCMonth day-clamping), doesn't touch vercel.json), p1-w3 `7a9f985d` (moved since 04:35: `booking_completed` notify-wiring gap closed + gap-doc update, doesn't touch vercel.json), p1-w4 `ac67803d` (moved since 04:35: telnyx-voice ring_strategy/do_not_disturb_until wiring fix + finance entities default-guard fix, neither touches vercel.json), p1-w5 `399cc29b` (unchanged, still Jul-13 18:23 — stale), p1-w6 `d956237a` (unchanged, still Jul-13 18:56 — stale). Re-diffed all 6 `vercel.json` crons arrays via `git show <branch>:platform/vercel.json` (hashed for equality, confirmed w2/w3/w4's moved commits touched none of the files this gap tracks via `git show --stat`): unchanged from the 04:35 check across every row — `seo-alerts` entry-only on w1/code-only on w2, `seo-index-cliff` entry+code solo on w1, `seo-health` + `hr-document-reminders` entry+code solo on w4, `gdpr-purge` still two independently-built implementations at their original schedules (w2 `gdpr-deletion.ts` @ `0 9 * * *`, w5 `gdpr.ts` @ `30 5 * * *`), w3/w6 still baseline, w1's `generate-monthly-invoices` + `refresh-job-postings`'s cheaper 1st/16th schedule both still w1-solo. No consolidation/merge activity detected on any branch per current LEADER-CHANNEL contents.

## Re-verify 05:10 (2026-07-17)

Re-verified live against current HEADs — p1-w1 `359c1d50` (this session's own commits since 04:53: `payment-followup-daily`'s 14-day recency floor naive-ET/UTC fix, then its consent-bypass fix (raw `sendSMS()` with zero `sms_consent` check — a client who'd replied STOP still got texted payment demands) — neither touches vercel.json or any file in this gap's scope), p1-w2 `f148e716` (moved since 04:53: a leader-run-only live-schema probe test for the `do_not_service` gate, docs/test-only, doesn't touch vercel.json), p1-w3 `7a9f985d` (unchanged since 04:48), p1-w4 `5c4c64ca` (moved since 04:53: Yinez admin-chat Telegram-channel-spoofing fix, doesn't touch vercel.json), p1-w5 `399cc29b` (unchanged, still Jul-13 18:23 — stale), p1-w6 `d956237a` (unchanged, still Jul-13 18:56 — stale). Re-diffed all 6 `vercel.json` crons arrays via `git show <branch>:platform/vercel.json` and re-confirmed every gap file via `git cat-file -e`: unchanged from the 04:53 check across every row — `seo-alerts` entry-only on w1/code-only on w2, `seo-index-cliff` entry+code solo on w1, `seo-health` + `hr-document-reminders` entry+code solo on w4, `gdpr-purge` still two independently-built implementations at their original schedules (w2 `gdpr-deletion.ts` @ `0 9 * * *`, w5 `gdpr.ts` @ `30 5 * * *`), w3/w6 still baseline, w1's `generate-monthly-invoices` + `refresh-job-postings`'s cheaper 1st/16th schedule both still w1-solo. No consolidation/merge activity detected on any branch per current LEADER-CHANNEL contents.

## Re-verify 05:39 (2026-07-17)

Re-verified live against current HEADs — p1-w1 `9f63e628` (this session's own commits since 05:11: `deals/at-risk`'s naive-ET-vs-true-UTC `start_time` comparison fix in the sales outreach feed, then `cron/generate-recurring`'s NYC-Maid auto-resume `paused_until`-vs-UTC-calendar-day fix — neither touches vercel.json or any file in this gap's scope), p1-w2 `4206e6f0`, p1-w3 `355107a1`, p1-w4 `5b2c4029`, p1-w5 `399cc29b` (unchanged, still Jul-13 18:23 — stale), p1-w6 `d956237a` (unchanged, still Jul-13 18:56 — stale). Re-diffed all 6 `vercel.json` crons arrays via `git show <branch>:platform/vercel.json` (hashed for equality) and re-confirmed every gap file via `git cat-file -e`: unchanged from the 05:10 check across every row — `seo-alerts` entry-only on w1/code-only on w2, `seo-index-cliff` entry+code solo on w1, `seo-health` + `hr-document-reminders` entry+code solo on w4, `gdpr-purge` still two independently-built implementations at their original schedules (w2 `gdpr-deletion.ts` @ `0 9 * * *`, w5 `gdpr.ts` @ `30 5 * * *`), w3/w6 still baseline, w1's `generate-monthly-invoices` + `refresh-job-postings`'s cheaper 1st/16th schedule both still w1-solo. No consolidation/merge activity detected on any branch per current LEADER-CHANNEL contents.

**Unrelated dead-code note found while fresh-ground hunting (flagging, not fixing):** `src/app/site/{nyc-mobile-salon,wash-and-fold-hoboken,wash-and-fold-nyc}/_lib/recurring.ts` (3 byte-identical clones, only their `filterHolidays` import path differs) each carry the same chained `monthly_date` `current.setMonth(current.getMonth()+1)` permanent-drift bug already fixed elsewhere tonight in `src/lib/recurring.ts` and `BookingsAdmin.tsx`'s `_recurring.ts` — but these 3 files' only consumer, each site's own `_components/RecurringOptions.tsx`, is never imported by anything (verified via repo-wide grep; these 3 tenants' live booking flow goes through the shared `/api/client/book`, which already maps `'monthly'`→`'monthly_date'` correctly). Orphaned dead code from before the GLOBAL RULE (per `platform/CLAUDE.md`) consolidated operator/booking UI — not a live bug, not fixed here. Whoever eventually does a dead-code sweep should pull these 3 `_lib/recurring.ts` + `_components/RecurringOptions.tsx` pairs (6 files) along with it.

## Re-verify 05:50 (2026-07-17)

Re-verified live against current HEADs — p1-w1 `a187d7aa` (this session's own commit since 05:39: a broad sweep of every `new Date().toISOString().split('T')[0]` "today" default/comparison found 5 more naive-ET/true-UTC call sites in fresh domains — `cleaners/[id]` unavailable-dates filter, `finance/expenses` create-date default, `admin/ai-chat` + `ai/assistant`'s `get_schedule_summary` AI tool (same tool, independently reimplemented in both), and `sale-to-recurring.ts`'s quote-conversion start-date default; none touch vercel.json or any file in this gap's scope), p1-w2 `cddfd78d` (moved since 05:39: emergency-archetype items 73/74 — per-tenant clone availability.ts day-boundary fix + customer-facing same-day date-picker fix, then a gap/fluidity refresh closing sms_consent gap #9 on cron/payment-reminder + bookings create/update/cancel — doesn't touch vercel.json), p1-w3 `2c06a027` (unchanged since 05:39 by content, same commits as w2 reflect a shared merge point in that lane's history — doesn't touch vercel.json), p1-w4 `a67bd1f0` (moved since 05:39: manage_recurring bookings-table fix + prior-session commit cleanup, doesn't touch vercel.json), p1-w5 `399cc29b` (unchanged, still Jul-13 18:23 — stale), p1-w6 `d956237a` (unchanged, still Jul-13 18:56 — stale). Re-diffed all 6 `vercel.json` crons arrays via `git show <branch>:platform/vercel.json`: unchanged from the 05:39 check across every row — `seo-alerts` entry-only on w1/code-only on w2, `seo-index-cliff` entry+code solo on w1, `seo-health` + `hr-document-reminders` entry+code solo on w4, `gdpr-purge` still two independently-built implementations at their original schedules (w2 `gdpr-deletion.ts` @ `0 9 * * *`, w5 `gdpr.ts` @ `30 5 * * *`), w3/w6 still baseline, w1's `generate-monthly-invoices` + `refresh-job-postings`'s cheaper 1st/16th schedule both still w1-solo. No consolidation/merge activity detected on any branch per current LEADER-CHANNEL contents.

## Re-verify 06:06 (2026-07-17)

Re-verified live against current HEADs — p1-w1 `da836397` (this session's own commit since 05:50: 3 fresh naive-ET/true-UTC call sites — `schedules/route.ts`'s day-of-week anchor search read the server's local (UTC) calendar instead of ET, `invoices/public/[token]` + `quotes/public/[token]`'s overdue/expired checks parsed a DATE column via UTC-midnight instead of comparing ET calendar dates; none touch vercel.json or any file in this gap's scope), p1-w2 `6e79ac59` (moved since 05:50: 4 more missing-consent-check call sites closed + a notify() structural-fix proposal doc, doesn't touch vercel.json — confirmed via `git diff --stat cddfd78d..6e79ac59`, touches only notifications/route.ts, send-booking-emails/route.ts, lib/types.ts + tests), p1-w3 `afe3a2f6` (moved since 05:50: client-side minDate 24hr-lead UTC fix + a referrals wrong-table-schema fix, doesn't touch vercel.json — confirmed via `git diff --stat 2c06a027..afe3a2f6`), p1-w4 `aa6608f5` (moved since 05:50: admin/recurring-schedules missed-'confirmed'-status fix across 5 routes, doesn't touch vercel.json — confirmed via `git diff --stat a67bd1f0..aa6608f5`), p1-w5 `399cc29b` (unchanged, still Jul-13 18:23 — stale), p1-w6 `d956237a` (unchanged, still Jul-13 18:56 — stale). Re-hashed all 6 `vercel.json` crons arrays via `git show <branch>:platform/vercel.json | md5` (all 6 distinct except w3==w6, matching the established baseline-match pattern) and re-confirmed every gap file via `git cat-file -e`: unchanged from the 05:50 check across every row — `seo-alerts` entry-only on w1/code-only on w2, `seo-index-cliff` entry+code solo on w1, `seo-health` + `hr-document-reminders` entry+code solo on w4, `gdpr-purge` still two independently-built implementations at their original schedules (w2 `gdpr-deletion.ts` @ `0 9 * * *`, w5 `gdpr.ts` @ `30 5 * * *`), w3/w6 still baseline, w1's `generate-monthly-invoices` + `refresh-job-postings`'s cheaper 1st/16th schedule both still w1-solo. No consolidation/merge activity detected on any branch per current LEADER-CHANNEL contents.

## Re-verify 06:16 (2026-07-17)

Re-verified live against current HEADs — p1-w1 `d497517f` (this session's own 2 commits since 06:06: `lib/pest-warranty.ts`'s `warrantyStatus()` default-`today` truncation read UTC calendar components instead of ET, then `POST /api/dashboard/treatments`'s omitted-`application_date` default used the true-UTC day instead of `nowNaiveET()`; neither touches vercel.json or any file in this gap's scope), p1-w2 `32e7dced` (moved since 06:06: a gap/fluidity refresh closing 3 more missing-consent-check call sites — cron/post-job-followup, cron/confirmations client day-before, cron/payment-followup-daily — doesn't touch vercel.json, confirmed via `git diff --stat 6e79ac59..32e7dced`, touches only those 3 cron routes + lib/types.ts + tests/docs/sim script), p1-w3 `afe3a2f6` (unchanged since 06:06), p1-w4 `04b8f48b` (moved since 06:06: pending-alert confirmed-status fix + stale team-confirm-across-reschedule fix on cron/confirmations, cron/reminders, webhooks/telnyx — doesn't touch vercel.json, confirmed via `git diff --stat aa6608f5..04b8f48b`), p1-w5 `399cc29b` (unchanged, still Jul-13 18:23 — stale), p1-w6 `d956237a` (unchanged, still Jul-13 18:56 — stale). Re-hashed all 6 `vercel.json` crons arrays via `git show <branch>:platform/vercel.json | md5` (all 6 distinct except w3==w6, matching the established baseline-match pattern) and re-confirmed every gap file via `git cat-file -e`: unchanged from the 06:06 check across every row — `seo-alerts` entry-only on w1/code-only on w2, `seo-index-cliff` entry+code solo on w1, `seo-health` + `hr-document-reminders` entry+code solo on w4, `gdpr-purge` still two independently-built implementations at their original schedules (w2 `gdpr-deletion.ts` @ `0 9 * * *`, w5 `gdpr.ts` @ `30 5 * * *`), w3/w6 still baseline, w1's `generate-monthly-invoices` + `refresh-job-postings`'s cheaper 1st/16th schedule both still w1-solo. No consolidation/merge activity detected on any branch per current LEADER-CHANNEL contents.

## Re-verify 06:39 (2026-07-17)

Re-verified live against current HEADs — p1-w1 `484357f5` (this session's own 2 commits since 06:16: `recurring-expenses` POST's omitted `start_date`/`next_due_date` default used the true-UTC day instead of `nowNaiveET()` — the anchor `cron/recurring-expenses`'s `advance()` (91919561) trusts as ground truth — then the entire real-time ledger-posting spine (`post-labor.ts`/`post-revenue.ts`/`post-adjustments.ts`, 7 call sites) had the same `entry_date` default bug; neither touches vercel.json or any file in this gap's scope), p1-w2 `ad1dd5d5` (moved since 06:16: `client/confirm/[token]` + `email/monitor`'s sms-consent-guard closures, doesn't touch vercel.json — confirmed via `git diff --stat 32e7dced..ad1dd5d5`, touches only those 2 routes + tests/docs/sim script), p1-w3 `a1dbca97` (moved since 06:16: a broad client-side UTC-vs-ET sweep across ~19 files — team-portal/auth, team/[id], dashboard/team/[id], and every book/dashboard/reschedule page's date-picker/minDate logic — doesn't touch vercel.json, confirmed via `git diff --stat afe3a2f6..a1dbca97`), p1-w4 `c74cffa9` (moved since 06:16: `cron/confirmations` + `cron/schedule-monitor` + `team-portal/crew/schedule`'s UTC-vs-ET today-boundary fixes, doesn't touch vercel.json — confirmed via `git diff --stat 04b8f48b..c74cffa9`), p1-w5 `399cc29b` (unchanged, still Jul-13 18:23 — stale), p1-w6 `d956237a` (unchanged, still Jul-13 18:56 — stale). Re-hashed all 6 `vercel.json` crons arrays via `git show <branch>:platform/vercel.json | md5` (all 6 distinct except w3==w6, matching the established baseline-match pattern) and re-confirmed every gap file via `git cat-file -e`: unchanged from the 06:16 check across every row — `seo-alerts` entry-only on w1/code-only on w2, `seo-index-cliff` entry+code solo on w1, `seo-health` + `hr-document-reminders` entry+code solo on w4, `gdpr-purge` still two independently-built implementations at their original schedules (w2 `gdpr-deletion.ts` @ `0 9 * * *`, w5 `gdpr.ts` @ `30 5 * * *`), w3/w6 still baseline, w1's `generate-monthly-invoices` + `refresh-job-postings`'s cheaper 1st/16th schedule both still w1-solo. No consolidation/merge activity detected on any branch per current LEADER-CHANNEL contents.

## Re-verify 06:47 (2026-07-17)

Re-verified live against current HEADs — p1-w1 `a415718d` (this session's own 2 commits since 06:39: `cron/reminders`' 9pm ET nightly digest built its `notifications.created_at` window from server-local (UTC) midnight instead of converting the ET day boundary via `parseNaiveET`, then `cron/payment-reminder`'s client-facing 15-30min nudge texted opted-out clients with no `sms_consent` check at all — sibling to the earlier payment-followup-daily fix; neither touches vercel.json or any file in this gap's scope), p1-w2 `2ba76278` (moved since 06:39: `clients/[id]` + `portal/notes` special-instructions field-wiring fix, doesn't touch vercel.json — confirmed via `git diff --stat ad1dd5d5..2ba76278`, touches only those 2 routes + tests/docs/sim script), p1-w3 `a1dbca97` (unchanged since 06:39), p1-w4 `dfca0f3c` (moved since 06:39: `admin/calendar` + `bookings/stats` + `team-portal/earnings`'s UTC-vs-ET today-boundary fixes, doesn't touch vercel.json — confirmed via `git diff --stat c74cffa9..dfca0f3c`), p1-w5 `399cc29b` (unchanged, still Jul-13 18:23 — stale), p1-w6 `d956237a` (unchanged, still Jul-13 18:56 — stale). Re-hashed all 6 `vercel.json` crons arrays via `git show <branch>:platform/vercel.json | md5` (all 6 distinct except w3==w6, matching the established baseline-match pattern) and re-confirmed every gap file via `git cat-file -e`: unchanged from the 06:39 check across every row — `seo-alerts` entry-only on w1/code-only on w2, `seo-index-cliff` entry+code solo on w1, `seo-health` + `hr-document-reminders` entry+code solo on w4, `gdpr-purge` still two independently-built implementations at their original schedules (w2 `gdpr-deletion.ts` @ `0 9 * * *`, w5 `gdpr.ts` @ `30 5 * * *`), w3/w6 still baseline, w1's `generate-monthly-invoices` + `refresh-job-postings`'s cheaper 1st/16th schedule both still w1-solo. No consolidation/merge activity detected on any branch per current LEADER-CHANNEL contents.

## Re-verify 07:10 (2026-07-17)

Re-verified live against current HEADs — p1-w1 (this session, uncommitted at check time: 5 finance report routes' entry_date/start_time range defaults re-anchored from server-UTC to ET -- summary.ts was a SELF-REGRESSION closure, its 04:53 fix assumed entry_date stayed UTC-written, invalidated by this session's own 06:38 ledger-posting fix -- plus leads/feed + leads/visits' "today" stat day-boundary fix; neither touches vercel.json or any file in this gap's scope), p1-w2 `2d783935` (moved since 06:47: `clients/[id]` PUT allowlist unit-field-wiring fix + sim-all-trades probe addition, doesn't touch vercel.json — confirmed via `git diff --stat 2ba76278..2d783935`), p1-w3 `8d2186fb` (moved since 06:47: uploads route + middleware-domain-lookup fixes plus an emergency/24-7 archetype-gaps doc, doesn't touch vercel.json — confirmed via `git diff --stat a1dbca97..8d2186fb`), p1-w4 `870e1229` (moved since 06:47: dashboard aggregator's today-boundary UTC-vs-ET fix, doesn't touch vercel.json — confirmed via `git diff --stat dfca0f3c..870e1229`), p1-w5 `399cc29b` (unchanged, still Jul-13 18:23 — stale), p1-w6 `d956237a` (unchanged, still Jul-13 18:56 — stale). Re-hashed all 6 `vercel.json` crons arrays via `git show <branch>:platform/vercel.json | shasum` (all 6 distinct except w3==w6, matching the established baseline-match pattern) and re-confirmed every gap file via `git cat-file -e`: unchanged from the 06:47 check across every row — `seo-alerts` entry-only on w1/code-only on w2, `seo-index-cliff` entry+code solo on w1, `seo-health` + `hr-document-reminders` entry+code solo on w4, `gdpr-purge` still two independently-built implementations at their original schedules (w2 `gdpr-deletion.ts`, w5 `gdpr.ts`), w3/w6 still baseline, w1's `generate-monthly-invoices` + `refresh-job-postings`'s cheaper 1st/16th schedule both still w1-solo. No consolidation/merge activity detected on any branch per current LEADER-CHANNEL contents.

## Re-verify 07:26 (2026-07-17)

Re-verified live against current HEADs — p1-w1 `72f7988d` (this session's own 2 commits since 07:10: 3 more client-facing crons/routes (`cron/confirmations`' day-before, `cron/post-job-followup`'s standalone+job branches, `POST /api/reviews/request`) closed for the sms_consent gate, then `lib/invoice.ts`'s `generateInvoiceNumber()` fixed for the same UTC-vs-ET class — neither touches vercel.json or any file in this gap's scope), p1-w2 `3a39157e` (moved since 07:10: `client/notes` field-mapping fix + `team-portal/preferences` field-wiring fix, doesn't touch vercel.json — confirmed via `git diff --stat 2d783935..3a39157e`), p1-w3 `0f69d4cc` (moved since 07:10: `bookings/[id]` explicit-unassign-to-null notification gap fix, doesn't touch vercel.json — confirmed via `git diff --stat 8d2186fb..0f69d4cc`), p1-w4 `519eda40` (moved since 07:10: `admin/analytics`+`admin/bookings`+`admin/finance`+`admin/leads`+`admin/websites`+`clients/stats`+`finance/revenue`+`finance/summary`'s UTC-vs-ET today-boundary fixes + a new `toNaiveET` helper, doesn't touch vercel.json — confirmed via `git diff --stat 870e1229..519eda40`), p1-w5 `399cc29b` (unchanged, still Jul-13 18:23 — stale), p1-w6 `d956237a` (unchanged, still Jul-13 18:56 — stale). Re-hashed all 6 `vercel.json` crons arrays via `git show <branch>:platform/vercel.json | shasum` (all 6 distinct except w3==w6, matching the established baseline-match pattern) and re-confirmed every gap file via `git cat-file -e`: unchanged from the 07:10 check across every row — `seo-alerts` entry-only on w1/code-only on w2, `seo-index-cliff` entry+code solo on w1, `seo-health` + `hr-document-reminders` entry+code solo on w4, `gdpr-purge` still two independently-built implementations at the same competing schedules (w2 `0 9 * * *` via `lib/gdpr-deletion.ts`, w5 `30 5 * * *` via `lib/gdpr.ts`), w3/w6 still baseline (identical crons array), w1's `generate-monthly-invoices` + `refresh-job-postings`'s cheaper 1st/16th schedule both still w1-solo. No consolidation/merge activity detected on any branch per current LEADER-CHANNEL contents.

## Re-verify 07:38 (2026-07-17)

Re-verified live against current HEADs — p1-w1 (this session, 2 commits since 07:26: `lib/notify.ts`'s shared 55-importer notification dispatcher fixed for the sms_consent gate (fresh ground, its `sendSMS()` fallback path was the one call site none of tonight's per-route consent fixes could reach), then 5 more dashboard/report routes (`admin/bookings`, `admin/leads`, `admin/websites`, `admin/finance`, `finance/revenue`) closed for the UTC-vs-ET today/week/month/year boundary class via a new `etDayBoundaryUTC()` helper; neither touches vercel.json or any file in this gap's scope), p1-w2 `1c68cf4e` (moved since 07:26: `team-portal/availability` field-wiring fix, doesn't touch vercel.json — confirmed via `git diff --stat 3a39157e..1c68cf4e`), p1-w3 `2bd54488` (moved since 07:26: `webhooks/stripe` deposit-fulfillment fix + an emergency/24-7 archetype-gaps doc update, doesn't touch vercel.json — confirmed via `git diff --stat 0f69d4cc..2bd54488`), p1-w4 `0fb8e084` (moved since 07:26: `cron/confirmations` UTC-vs-ET boundary + client day-before ET-hour fixes, doesn't touch vercel.json — confirmed via `git diff --stat e265f57e..0fb8e084`), p1-w5 `399cc29b` (unchanged, still Jul-13 18:23 — stale), p1-w6 `d956237a` (unchanged, still Jul-13 18:56 — stale). Re-hashed all 6 `vercel.json` crons arrays via `git show <branch>:platform/vercel.json | shasum` (all 6 distinct except w3==w6, matching the established baseline-match pattern) and re-confirmed every gap file via `git cat-file -e`: unchanged from the 07:26 check across every row — `seo-alerts` entry-only on w1/code-only on w2, `seo-index-cliff` entry+code solo on w1, `seo-health` + `hr-document-reminders` entry+code solo on w4, `gdpr-purge` still two independently-built implementations at the same competing schedules (w2 `lib/gdpr-deletion.ts`, w5 `lib/gdpr.ts`), w3/w6 still baseline (identical crons array), w1's `generate-monthly-invoices` + `refresh-job-postings`'s cheaper 1st/16th schedule both still w1-solo. No consolidation/merge activity detected on any branch per current LEADER-CHANNEL contents.

## Re-verify 08:35 (2026-07-17)

Re-verified live against current HEADs — p1-w1 `bf12ac03` (this session's own 2 commits since 08:27: `quotes/[id]/convert` fixed twice -- dollars-vs-cents on `bookings.price` (3rd instance of tonight's dollars/cents class, matching `lib/sale-to-booking.ts`'s convention exactly), then a fresh-ground find, the 4th instance of tonight's fulfillment-routing gap: the staff "Convert to Booking" button always created a one-off Booking regardless of `quote.recurring_type`, now delegates to `lib/sale-to-recurring.ts` when set, plus a matching frontend redirect fix -- neither touches vercel.json or any file in this gap's scope), p1-w2 `e680214a` (moved since 08:19: sensitive-column-exposure fresh-ground find+fix -- `admin/tenants/[id]` + `admin/businesses/[id]` leaked `tenant_members.pin_hash` via unredacted `select('*')`, plus a live-schema archetype probe and a gap/fluidity doc refresh; doesn't touch vercel.json — confirmed via `git diff --stat` against the prior recorded SHA), p1-w3 `145d8e9c` (moved since 08:19: item (92) archetype depth -- `deals/[id]/stage`'s manual Kanban close-to-Sold had the same fulfillment-routing gap item (87) fixed on the Stripe webhook, now fixed; item (93) fresh ground -- Selena's `update_deal` AI tool wrote to a nonexistent `value` column (real column is `value_cents`, every AI-driven deal-value update had errored since inception) and skipped every human-close side effect when closing a deal to Sold via chat; doesn't touch vercel.json), p1-w4 `d93f0c98` (moved since 08:19: `cron/system-check`, `cron/health-check`, `cron/lifecycle`, `cron/outreach`, `cron/payment-followup-daily` all closed for the same naive-ET-vs-UTC boundary class; doesn't touch vercel.json), p1-w5 `399cc29b` (unchanged, still Jul-13 18:23 — stale), p1-w6 `d956237a` (unchanged, still Jul-13 18:56 — stale). Re-hashed all 6 `vercel.json` crons arrays via `git show <branch>:platform/vercel.json | shasum` (all 6 distinct except w3==w6, matching the established baseline-match pattern) and re-confirmed every gap file via `git cat-file -e`: unchanged from the 07:38 check across every row — `seo-alerts` entry-only on w1/code-only on w2, `seo-index-cliff` entry+code solo on w1, `seo-health` + `hr-document-reminders` entry+code solo on w4, `gdpr-purge` still two independently-built implementations at the same competing schedules (w2 `lib/gdpr-deletion.ts` @ `0 9 * * *`, w5 `lib/gdpr.ts` @ `30 5 * * *`), w3/w6 still baseline (identical crons array), w1's `generate-monthly-invoices` + `refresh-job-postings`'s cheaper 1st/16th schedule both still w1-solo. No consolidation/merge activity detected on any branch per current LEADER-CHANNEL contents.

## Re-verify 09:05 (2026-07-17)

Re-verified live against current HEADs — p1-w1 `116f6cea` (this session's own commit since 08:55: `cron/schedule-monitor`'s main 14-day booking window (+ no-show lower bound + stale-issue reconcile) built `todayStr`/`endDateStr` from server-UTC `new Date()` calendar getters instead of ET -- a leftover unfixed call site in a file that already imports/uses `nowNaiveET()` correctly elsewhere; fixed via `etToday()`/`addCalendarDays()`/`formatNaiveET()`; doesn't touch vercel.json or any file in this gap's scope), p1-w2 `4cee9a49` (moved since 08:55: gap/fluidity refresh -- fresh-ground sweep extended the pin_hash bug class to `clients.pin`/`team_members.pin`, 3 real instances found+fixed (most severe: crew PIN leaked to client via reschedule); doesn't touch vercel.json — confirmed via `git diff --stat e680214a..4cee9a49`), p1-w3 `9d8c5f82` (moved since 08:55: item (94)-(97) — Selena's owner-facing `tools.ts` + `core.ts` never ported off nycmaid's pre-rename `cleaners`/`cleaner_id` vocabulary across ~14 + ~8 tool handlers (including the canonical `score_cleaners` availability source), plus a lead/series-reassignment notify-gap fix; doesn't touch vercel.json — confirmed via `git diff --stat 145d8e9c..9d8c5f82`), p1-w4 `479a003c` (moved since 08:55: `dates.ts`'s `naiveETToUtc()` addition, `schedule/calendar` + `recurring-schedules` naive-ET-vs-UTC boundary fixes, then atomic-claim double-fire-SMS fixes on `team-portal/running-late` + `team-portal/15min-alert`; doesn't touch vercel.json — confirmed via `git diff --stat d93f0c98..479a003c`), p1-w5 `399cc29b` (unchanged, still Jul-13 18:23 — stale), p1-w6 `d956237a` (unchanged, still Jul-13 18:56 — stale). Re-hashed all 6 `vercel.json` crons arrays via `git show <branch>:platform/vercel.json | shasum` (all 6 distinct except w3==w6, matching the established baseline-match pattern) and re-confirmed every gap file via `git cat-file -e`: unchanged from the 08:35 check across every row — `seo-alerts` entry-only on w1/code-only on w2, `seo-index-cliff` entry+code solo on w1, `seo-health` + `hr-document-reminders` entry+code solo on w4, `gdpr-purge` still two independently-built implementations at the same competing schedules (w2 `lib/gdpr-deletion.ts` @ `0 9 * * *`, w5 `lib/gdpr.ts` @ `30 5 * * *`), w3/w6 still baseline (identical crons array), w1's `generate-monthly-invoices` + `refresh-job-postings`'s cheaper 1st/16th schedule both still w1-solo. No consolidation/merge activity detected on any branch per current LEADER-CHANNEL contents.

## Re-verify 09:26 (2026-07-17)

Re-verified live against current HEADs — p1-w1 `4f9db9e2` (this session's own 2 commits since 09:10, under the new leader (session-17): `schedules/route.ts`'s missing-`preferred_time` fallback read the server's UTC clock instead of ET wall-clock (`nowNaiveET()`), then the same route's bookings batch-insert error was discarded entirely, reporting false 201 success when the DB's overlap trigger aborted the whole insert; neither touches vercel.json or any file in this gap's scope), p1-w2 `b604e580` (moved since 08:55/4cee9a49: fresh-ground sweep extended the `select(*)`-leak bug class to `tenants` (vendor API keys + `google_tokens`), 2 real instances found+fixed on `admin/tenants/[id]` + `admin/businesses/[id]`; doesn't touch vercel.json — confirmed via `git diff --stat 4cee9a49..b604e580`, touches only `admin/businesses`/`admin/tenants`/`client/book`/`client/booking`/`client/bookings`/`client/reschedule` + their tests), p1-w3 `d463dc6f` (moved since 08:55/9d8c5f82: items (97)/(98) — reassignment-notify gap closed on `bookings/[id]/team` + `bookings/batch`, then `cron/payment-followup-daily`'s refunded-client double-ask bug fixed; doesn't touch vercel.json — confirmed via `git diff --stat 9d8c5f82..d463dc6f`), p1-w4 `4154a2de` (moved since 08:55/479a003c: 3 more naive-ET-vs-UTC boundary bugs — job-claim daily cap, public availability same-day gate, Selena calendar context; doesn't touch vercel.json — confirmed via `git diff --stat 479a003c..4154a2de`), p1-w5 `399cc29b` (unchanged, still Jul-13 18:23 — stale), p1-w6 `d956237a` (unchanged, still Jul-13 18:56 — stale). Re-hashed all 6 `vercel.json` crons arrays via `git show <branch>:platform/vercel.json | shasum` (all 6 distinct except w3==w6, matching the established baseline-match pattern) and re-confirmed every gap file via `git cat-file -e`: unchanged from the 09:05 check across every row — `seo-alerts` entry-only on w1/code-only on w2, `seo-index-cliff` entry+code solo on w1, `seo-health` + `hr-document-reminders` entry+code solo on w4, `gdpr-purge` still two independently-built implementations at the same competing schedules (w2 `lib/gdpr-deletion.ts` @ `0 9 * * *`, w5 `lib/gdpr.ts` @ `30 5 * * *`), w3/w6 still baseline (identical crons array), w1's `generate-monthly-invoices` + `refresh-job-postings`'s cheaper 1st/16th schedule both still w1-solo. No consolidation/merge activity detected on any branch per current LEADER-CHANNEL contents.

## Re-verify 09:47 (2026-07-17) — NEW row: w3's w3==w6 baseline match broke

Re-verified live against current HEADs — p1-w1 `7689e9f0` (this session's own 2 commits since 09:26: 2 sibling `booking_assignees` insert-error-discarded instances closed on `POST /api/jobs/[id]/sessions` + its `PATCH [sessionId]` sibling (same silent-write-failure class as the `schedules` fix just landed), then `recurring_type: 'custom'` schedules fixed to actually refill — a new `custom_interval_days` column + backfill (this session's own schema+backfill lane) plus `lib/recurring.ts`/both schedule-writer routes/`cron/generate-recurring` wired through; neither touches vercel.json or any file in this gap's scope), p1-w2 `d67bd30b` (moved since 09:26: gap/fluidity refresh, continued the `select(*)`-leak sweep — found the actual prior-round grep-pattern gap (`select('*, joined)` not matched by an exact `select('*')` grep), 2 more real instances fixed (`admin/tenants`, `admin/businesses` LIST routes); doesn't touch vercel.json — confirmed via `git diff --stat b604e580..d67bd30b`, touches only `admin/tenants`/`admin/businesses` + docs), p1-w3 `8454eae0` (moved since 09:26: items (99)-(102) — a scheduled-campaigns dispatch cron (**new vercel.json entry, see below**), a Yinez internal-error admin-notify gap, confirmed-booking email price omission across non-nycmaid tenants, and Resend's `email.complained` spam-complaint event having zero handling; doesn't touch anything else in this gap's tracked rows — confirmed via `git diff --stat d463dc6f..8454eae0 -- platform/vercel.json` showing exactly the one new entry, nothing else), p1-w4 `d551e24f` (moved since 09:26: atomic terminal-status guards on admin bookings PUT/PATCH + client/portal booking mutation routes, doesn't touch vercel.json — confirmed via `git diff --stat 4154a2de..d551e24f`), p1-w5 `399cc29b` (unchanged, still Jul-13 18:23 — stale), p1-w6 `d956237a` (unchanged, still Jul-13 18:56 — stale).

Re-hashed all 6 `vercel.json` crons arrays via `git show <branch>:platform/vercel.json | shasum`: **the established w3==w6 baseline-match broke this round** — w3's hash now differs from w6's for the first time in this doc's history. Diffed directly: w3 gained one new crons entry, `{ "path": "/api/cron/campaign-dispatch", "schedule": "*/15 * * * *" }`, absent on w6 (and on w1/w2/w4/w5 — confirmed via `git cat-file -e <branch>:platform/src/app/api/cron/campaign-dispatch/route.ts` on all 6). Same shape as the existing `seo-health`/`hr-document-reminders` rows (entry+code solo on one branch, no schema change needed — `campaigns.status`/`scheduled_at` are pre-existing columns this route only reads). Adding to the tracked table:

| Entry | Present on | Notes |
|---|---|---|
| `campaign-dispatch` (`*/15 * * * *`) | w3 only | Route confirmed present on p1-w3 (commit `a6c137d0`, "scheduled campaigns had no dispatch path and could never populate the Scheduled tab"). Absent on w1/w2/w4/w5/w6. No migration gap — reads/writes only pre-existing `campaigns` columns. |

Every other previously-tracked row is unchanged: `seo-alerts` entry-only on w1/code-only on w2, `seo-index-cliff` entry+code solo on w1, `seo-health` + `hr-document-reminders` entry+code solo on w4, `gdpr-purge` still two independently-built implementations at the same competing schedules (w2 `lib/gdpr-deletion.ts` @ `0 9 * * *`, w5 `lib/gdpr.ts` @ `30 5 * * *`), w1's `generate-monthly-invoices` + `refresh-job-postings`'s cheaper 1st/16th schedule both still w1-solo (re-confirmed w1's own vercel.json is byte-identical to the 09:26 check via `git diff --stat 4f9db9e2..p1-w1 -- platform/vercel.json`, empty). No consolidation/merge activity detected on any branch per current LEADER-CHANNEL contents.

## Re-verify 09:53 (2026-07-17)

Re-verified live against current HEADs — p1-w1 `645bf28b` (this session's own commit since 09:47: this doc's own 09:47 re-verify entry, no code — doesn't touch vercel.json or any other file in this gap's scope), p1-w2 `beb1e800` (moved since 09:47/`d67bd30b`: docs-only, item-1 gap/fluidity report for the 76-site explicit-secret-column-select inventory sweep — doesn't touch vercel.json, confirmed via `git diff --stat d67bd30b..beb1e800`, one new file under `deploy-prep/`), p1-w3 `6cbdb6ae` (moved since 09:47/`8454eae0`: docs-only, this session's own 24-7 archetype-gaps-and-friction doc update — doesn't touch vercel.json, confirmed via `git diff --stat 8454eae0..6cbdb6ae`, one file changed), p1-w4 `d551e24f` (unchanged since 09:47), p1-w5 `399cc29b` (unchanged, still Jul-13 18:23 — stale), p1-w6 `d956237a` (unchanged, still Jul-13 18:56 — stale). Re-hashed all 6 `vercel.json` crons arrays via `git show <branch>:platform/vercel.json | shasum` and re-confirmed every gap file via `git cat-file -e`: unchanged from the 09:47 check across every row, including the new `campaign-dispatch` (w3-solo) entry added that round — `seo-alerts` entry-only on w1/code-only on w2, `seo-index-cliff` entry+code solo on w1, `seo-health` + `hr-document-reminders` entry+code solo on w4, `campaign-dispatch` entry+code solo on w3, `gdpr-purge` still two independently-built implementations at the same competing schedules (w2 `lib/gdpr-deletion.ts` @ `0 9 * * *`, w5 `lib/gdpr.ts` @ `30 5 * * *`), w1's `generate-monthly-invoices` + `refresh-job-postings`'s cheaper 1st/16th schedule both still w1-solo. w3's baseline-match with w6 remains broken (as of 09:47) — w3's hash (`7ce32518`) still differs from w6's (`6fb7dc4e`) by exactly the `campaign-dispatch` entry; w1's hash (`734ecb32`) remains distinct from all others as established. No consolidation/merge activity detected on any branch per current LEADER-CHANNEL contents.

## Re-verify 10:58 (2026-07-17)

Re-verified live against current HEADs — p1-w1 `19e781ff` (this session's own commit since 09:53: booking_team_members lead-sync gap closed in `team-portal/jobs/reassign` — same class fixed earlier this session in `cron/generate-recurring`'s refill, the regenerate route, and the admin exception 'reassign' path, now found in the field-portal reassign path too; doesn't touch vercel.json or any file in this gap's scope), p1-w2 `2220ce0d` (moved since 09:53/`beb1e800`: doesn't touch vercel.json, confirmed via `git diff --stat beb1e800..2220ce0d`), p1-w3 `d5e9cf1f` (moved since 09:53/`6cbdb6ae`: doesn't touch vercel.json, confirmed via `git diff --stat 6cbdb6ae..d5e9cf1f`), p1-w4 `699692c5` (moved since 09:53/`d551e24f`: doesn't touch vercel.json, confirmed via `git diff --stat d551e24f..699692c5`), p1-w5 `399cc29b` (unchanged, still Jul-13 18:23 — stale), p1-w6 `d956237a` (unchanged, still Jul-13 18:56 — stale). Re-hashed all 6 `vercel.json` crons arrays via `git show <branch>:platform/vercel.json | shasum` and re-confirmed every gap file via `git cat-file -e`: unchanged from the 09:53 check across every row — `seo-alerts` entry-only on w1/code-only on w2, `seo-index-cliff` entry+code solo on w1, `seo-health` + `hr-document-reminders` entry+code solo on w4, `campaign-dispatch` entry+code solo on w3, `gdpr-purge` still two independently-built implementations at the same competing schedules (w2 `lib/gdpr-deletion.ts` @ `0 9 * * *`, w5 `lib/gdpr.ts` @ `30 5 * * *`), w3/w6 baseline-match still broken from `campaign-dispatch`, w1's `generate-monthly-invoices` + `refresh-job-postings`'s cheaper 1st/16th schedule both still w1-solo. No consolidation/merge activity detected on any branch per current LEADER-CHANNEL contents.

## Re-verify 11:36 (2026-07-17)

Re-verified live against current HEADs — p1-w1 `fd5a28c1` (this session's own 5 commits since 10:58/`19e781ff`: the booking_team_members-sync gap fully swept — 4 more live write sites (`bookings/batch-update`, `admin/recurring-schedules/[id]`, `team-portal/jobs/release`, `admin/schedule-issues/fix`) plus a NOT NULL tenant_id regression fix in 2 earlier-fixed sites, then 2 more sibling instances in the separate `admin/ai-chat` CRM-copilot tool file (`update_bookings`, `create_booking`); none touch vercel.json or any file in this gap's scope — confirmed via `git diff --stat 19e781ff..fd5a28c1 -- platform/vercel.json`, empty), p1-w2 `5d6a4a40` (moved since 10:58/`2220ce0d`: doesn't touch vercel.json, confirmed via `git diff --stat 2220ce0d..5d6a4a40 -- platform/vercel.json`, empty), p1-w3 `1a2305ad` (moved since 10:58/`d5e9cf1f`: doesn't touch vercel.json, confirmed via `git diff --stat d5e9cf1f..1a2305ad -- platform/vercel.json`, empty), p1-w4 `4303ac6a` (moved since 10:58/`699692c5`: doesn't touch vercel.json, confirmed via `git diff --stat 699692c5..4303ac6a -- platform/vercel.json`, empty), p1-w5 `399cc29b` (unchanged, still Jul-13 18:23 — stale), p1-w6 `d956237a` (unchanged, still Jul-13 18:56 — stale). All 6 diff checks against the prior recorded SHAs came back empty for `platform/vercel.json`, confirming no crons-array drift this round — `seo-alerts` entry-only on w1/code-only on w2, `seo-index-cliff` entry+code solo on w1, `seo-health` + `hr-document-reminders` entry+code solo on w4, `campaign-dispatch` entry+code solo on w3, `gdpr-purge` still two independently-built implementations at the same competing schedules (w2 `lib/gdpr-deletion.ts` @ `0 9 * * *`, w5 `lib/gdpr.ts` @ `30 5 * * *`), w3/w6 baseline-match still broken from `campaign-dispatch`, w1's `generate-monthly-invoices` + `refresh-job-postings`'s cheaper 1st/16th schedule both still w1-solo. No consolidation/merge activity detected on any branch per current LEADER-CHANNEL contents.

## Re-verify 13:14 (2026-07-17)

## Real, currently-inert finding: 3 per-tenant `site/*/_lib` forks would become a cross-tenant leak if mechanically patched (per LEADER 13:16 direction — logging here, NOT fixing)

Separate from the vercel.json/crons gap this doc otherwise tracks — logging per LEADER's explicit 13:16 instruction to keep this a permanent record for whoever consolidates.

`src/app/site/{nyc-mobile-salon,wash-and-fold-hoboken,wash-and-fold-nyc}/_lib/{geo,availability,notify-cleaner,smart-schedule}.ts` are independently-diverged forks of the already-fixed `nycmaid/*` libs (own supabase import, own constants — not simple copies). They carry the same `cleaners`/`cleaner_id`/`active`-boolean schema-mismatch class already fixed everywhere else this session (`team_members`/`team_member_id`/`status='active'` is the real live schema). Confirmed all 3 forks point at the **same** live Supabase project as the main platform (identical `NEXT_PUBLIC_SUPABASE_URL`/`SUPABASE_SERVICE_ROLE_KEY` env vars, single deployment) — not separate per-tenant DBs.

**Why this is dangerous to fix mechanically:** unlike `nycmaid/*` (which filters every query by `tenant_id`), none of these 3 forks pass or filter by `tenant_id` anywhere in their call chain — traced every caller down to each fork's `selena.ts`, zero `tenant_id`/`tenantId` references anywhere in any of the 3 forks' `_lib` dirs, and no tenant-id constant/lookup to hang one on. `team_members`/`bookings` are genuinely multi-tenant shared tables.

**Current state (safe, inert):** `.from('cleaners')` errors because the table doesn't exist; the code destructures only `{data}` and never checks `error`, so results come back `null` and every function silently degrades to "no availability." Broken, but fail-closed and NOT cross-tenant.

**The trap:** applying the same mechanical rename used for `nycmaid/*` (`cleaners`→`team_members`, `cleaner_id`→`team_member_id`, `active`→`status='active'`) **without** also adding tenant scoping would flip this from "always shows no availability" into actively pulling **other tenants'** team members and bookings onto these 3 live public booking pages + their Selena AI agents — a real cross-tenant data leak, not just a naming fix.

**LEADER's 13:16 decision: option (c) — leave all 3 forks alone.** Do not fix names, do not add tenant scoping, as a broad-hunt queue item. `platform/CLAUDE.md` already lists per-tenant site clones as known debt slated for migration-to-global-then-deletion, not an ongoing maintenance target — and the "fix names only" middle option would be actively dangerous (converts a currently-safe fail-closed bug into a live leak the moment it's merged). If real tenant-scoping work on these forks is ever warranted, it needs its own deliberate pass, not a mechanical rename. Jeff flagged separately outside this channel per LEADER.

**Affected files (unmodified, do not touch without a dedicated tenant-scoping plan):**
- `src/app/site/nyc-mobile-salon/_lib/{geo,availability,notify-cleaner,smart-schedule}.ts`
- `src/app/site/wash-and-fold-hoboken/_lib/{geo,availability,notify-cleaner,smart-schedule}.ts`
- `src/app/site/wash-and-fold-nyc/_lib/{geo,availability,notify-cleaner,smart-schedule}.ts`

## 13:47 audit (2026-07-17): the 3 forks are confirmed DEAD CODE, not live — correcting the 13:22 entry's "live" characterization

Per LEADER's fresh dispatch: "audit the 3 per-tenant forks one at a time, confirm live-vs-dead and correct DB target before fixing each." Traced every call site for all 3 forks' `_lib/{geo,availability,notify-cleaner,smart-schedule,selena}.ts`. Finding: **the schema-mismatched code paths (`checkAvailability`, `getSmartSuggestions`, `scoreCleanersForBooking`, `notifyCleaner`, `checkCleanerAvailability`, `geocodeCleaner`) are unreachable from any live route, cron, or API in this codebase, in all 3 forks.** This corrects the 13:22 entry above, which called these "3 live public booking pages" — that characterization was inherited from the original finding without verifying the call chain past each fork's own `selena.ts`.

**Trace, per fork (identical result for all 3 — nyc-mobile-salon, wash-and-fold-hoboken, wash-and-fold-nyc):**
1. `availability.ts`/`smart-schedule.ts`/`notify-cleaner.ts` are imported by exactly one file each: the fork's own `_lib/selena.ts`. No other file in the repo imports them (`grep -rln` for the exported function names, whole repo).
2. `_lib/selena.ts` itself has **zero importers anywhere in the codebase** — not from a page, not from an API route, not from a cron, not from any other `_lib` file. `git grep`-equivalent search for every plausible import-path form came back empty across all 3 forks.
3. Each fork's `AiAssistant.tsx` component (the only plausible live entry point that would invoke a per-tenant Selena chat) is itself **never rendered** by any page/layout in its own site tree — and even if it were, it fetches the GLOBAL `/api/admin/ai-chat` endpoint, not the per-tenant fork's `selena.ts`.
4. The 3 `_components/{DashboardMap,ClientsMap,CleanerJobsMap}.tsx` files that also import from `_lib/geo.ts` only pull `geocodeAddress` (a pure Radar API call + pin math, no `cleaners` table access) — none call `geocodeCleaner`, the one function in `geo.ts` that touches the `cleaners` table.
5. `book/page.tsx` in each fork does not call `checkAvailability`/`getSmartSuggestions`/`scoreCleanersForBooking` anywhere — confirmed via direct grep for those call sites, no hits outside the `_lib` files that define them.
6. Cross-checked against the real, live Selena/Yinez path: `src/lib/selena/core.ts` + `agent.ts` (already fixed this session, `cb22ad02`) is the actual production SMS-agent engine, DB-driven and tenant-scoped via `agent-config-loader` + `tenants.selena_config`. `src/lib/selena/tenants/nyc-mobile-salon.ts` and `wash-and-fold-nyc.ts` exist as authored per-tenant persona overrides for *that* global engine — their own comments explicitly note they "mirror" (i.e. were manually copied from, not import) the dead per-tenant `_lib/selena.ts` prompt text. This confirms the per-tenant `_lib/selena.ts` forks were superseded by the global engine and never deleted, rather than being an active, reachable code path.

**Conclusion — no fix applied, files unmodified, consistent with LEADER's 13:16 "leave alone" decision but for a stronger reason:** these aren't "live pages with a fail-closed schema bug" — they're fully orphaned duplicate implementations with no live caller at all. Applying the `cleaners`→`team_members` mechanical rename here would touch dead code (zero runtime effect either way) while doing nothing to address the missing `tenant_id` scoping the 13:22 entry already flagged as the real danger if this code is ever wired up later. Renaming would also risk creating a false "already fixed" signal for code that's still unsafe to resurrect as-is.

**Correct DB target: confirmed same live Supabase project as main platform** (re-confirmed `NEXT_PUBLIC_SUPABASE_URL`/`SUPABASE_SERVICE_ROLE_KEY` env resolution identical across all 3 forks' `_lib/supabase.ts`, same as the 13:22 entry's finding — no change here).

**Recommendation for LEADER/Jeff, not actioned here (exceeds this queue item's scope):** since this is now confirmed fully dead rather than fail-closed-but-live, and `platform/CLAUDE.md` already lists this class as debt "slated for migration-to-global-then-deletion, not an ongoing maintenance target," deletion of the dead files (`selena.ts`, `availability.ts`, `notify-cleaner.ts`, `smart-schedule.ts`, and just the `geocodeCleaner` export from `geo.ts` — the rest of `geo.ts` is live, used by the Map components) is now a lower-risk cleanup option than it was under the original "might be live" premise. Left undone here since it's a deliberate deletion call beyond "confirm live-vs-dead," not a schema fix, and the prior explicit LEADER decision was to leave these files untouched.

**Files audited, unmodified:**
- `src/app/site/nyc-mobile-salon/_lib/{geo,availability,notify-cleaner,smart-schedule,selena}.ts`
- `src/app/site/wash-and-fold-hoboken/_lib/{geo,availability,notify-cleaner,smart-schedule,selena}.ts`
- `src/app/site/wash-and-fold-nyc/_lib/{geo,availability,notify-cleaner,smart-schedule,selena}.ts`

## Re-verify 13:22 (2026-07-17)

Re-verified live against current HEADs — p1-w1 `13706db3` (unchanged since 13:14 — this doc's own prior re-verify commit doesn't touch vercel.json). Re-diffed all 6 `vercel.json` crons arrays via `git show <branch>:platform/vercel.json`: unchanged from the 13:14 check across every row — `seo-alerts` entry-only on w1/code-only on w2, `seo-index-cliff` entry+code solo on w1, `seo-health` + `hr-document-reminders` entry+code solo on w4, `campaign-dispatch` entry+code solo on w3, `gdpr-purge` still two independently-built implementations at the same competing schedules (w2 `lib/gdpr-deletion.ts` @ `0 9 * * *`, w5 `lib/gdpr.ts` @ `30 5 * * *`), w3/w6 baseline-match still broken from `campaign-dispatch`, w1's `generate-monthly-invoices` + `refresh-job-postings`'s cheaper 1st/16th schedule both still w1-solo. No consolidation/merge activity detected on any branch per current LEADER-CHANNEL contents. Fork finding (above) added per LEADER's 13:16 instruction, not previously logged in this doc.

## Re-verify 13:58 (2026-07-17)

Re-verified live against current HEADs — p1-w1 `347ae07d` (this session's own commit since 13:22/`13706db3`: 3-fork dead-code audit above, docs-only, doesn't touch vercel.json), p1-w2 `b2edcb24` (moved since 13:22/`1719971c`: doesn't touch vercel.json, confirmed via `git diff --stat 1719971c..b2edcb24 -- platform/vercel.json`, empty), p1-w3 `ad59f91d` (moved since 13:22/`583b343a`: doesn't touch vercel.json, confirmed via `git diff --stat 583b343a..ad59f91d -- platform/vercel.json`, empty), p1-w4 `2bac5a01` (moved since 13:22/`03e32330`: doesn't touch vercel.json, confirmed via `git diff --stat 03e32330..2bac5a01 -- platform/vercel.json`, empty), p1-w5 `399cc29b` (unchanged, still Jul-13 18:23 — stale), p1-w6 `d956237a` (unchanged, still Jul-13 18:56 — stale). Re-hashed all 6 `vercel.json` crons arrays via `git show <branch>:platform/vercel.json | shasum`: all unchanged from the 13:22 round (w1 `734ecb32`, w2 `95b43932`, w3 `7ce32518`, w4 `2710f503`, w5 `e20953f5` stale, w6 `6fb7dc4e`). Every tracked row unchanged. No consolidation/merge activity detected on any branch per current LEADER-CHANNEL contents.

Fresh-ground note: while auditing the 3 dead site-fork libs above, traced the adjacent `src/lib/nycmaid/{push,notify-cleaner,smart-schedule}.ts` cluster (same `cleaners`/`cleaner_id` schema-mismatch class already fixed elsewhere this session) since `notify.ts` — which IS live, wired through `core.ts`/`tools.ts`/`/api/yinez` — imports `sendPushToAll` from `nycmaid/push.ts`. Result: `sendPushToAll` itself is schema-clean (filters by `role`, a real live `push_subscriptions` column per `/api/push/subscribe/route.ts`'s write path). But `sendPushToCleaner`/`sendPushToAllCleaners` in the same file use the nonexistent `cleaner_id` column (real column is `team_member_id`, confirmed against both the live global `src/lib/push.ts`'s `sendPushToTeamMember` and the subscribe route's actual insert/update columns) — same bug class, but confirmed **dead**: `notify-cleaner.ts` (the only caller of `sendPushToCleaner`) has zero importers anywhere, and `sendPushToAllCleaners` has zero callers at all outside the 3 already-audited dead site forks' own separate copies. No fix applied — nothing live to fix, consistent with this round's overall finding.

Re-verified live against current HEADs — p1-w1 `3511d47c` (this session's own commit since 11:36/`fd5a28c1`: BookingsAdmin.tsx's `cleaner_token`→`worker_token` rename from the prior round corrected to `team_member_token` — the actual live column per the 09:06 verified-schema finding, `worker_token` has zero writers anywhere; doesn't touch vercel.json — confirmed via `git diff --stat fd5a28c1..3511d47c -- platform/vercel.json`, empty), p1-w2 `1719971c` (moved since 11:36/`5d6a4a40`: doesn't touch vercel.json, confirmed via `git diff --stat 5d6a4a40..1719971c -- platform/vercel.json`, empty), p1-w3 `583b343a` (moved since 11:36/`1a2305ad`: doesn't touch vercel.json, confirmed via `git diff --stat 1a2305ad..583b343a -- platform/vercel.json`, empty), p1-w4 `03e32330` (moved since 11:36/`4303ac6a`: doesn't touch vercel.json, confirmed via `git diff --stat 4303ac6a..03e32330 -- platform/vercel.json`, empty), p1-w5 `399cc29b` (unchanged, still Jul-13 18:23 — stale), p1-w6 `d956237a` (unchanged, still Jul-13 18:56 — stale). Re-hashed all 6 `vercel.json` crons arrays via `git show <branch>:platform/vercel.json | shasum`: w1 `734ecb32` (unchanged), w2 `95b43932`, w3 `7ce32518` (unchanged), w4 `2710f503`, w5 `e20953f5` (unchanged, stale), w6 `6fb7dc4e` (unchanged) — all match prior-round values where previously recorded, confirming no crons-array drift. Every tracked row unchanged: `seo-alerts` entry-only on w1/code-only on w2, `seo-index-cliff` entry+code solo on w1, `seo-health` + `hr-document-reminders` entry+code solo on w4, `campaign-dispatch` entry+code solo on w3, `gdpr-purge` still two independently-built implementations at the same competing schedules (w2 `lib/gdpr-deletion.ts` @ `0 9 * * *`, w5 `lib/gdpr.ts` @ `30 5 * * *`), w3/w6 baseline-match still broken from `campaign-dispatch`, w1's `generate-monthly-invoices` + `refresh-job-postings`'s cheaper 1st/16th schedule both still w1-solo. No consolidation/merge activity detected on any branch per current LEADER-CHANNEL contents.

## Re-verify 14:20 (2026-07-17)

Re-hashed all 6 `vercel.json` crons arrays via `git show <branch>:platform/vercel.json | shasum` against current HEADs (p1-w1 `142b7bdc`, p1-w2 `a6517452`, p1-w3 `d974ccd4`, p1-w4 `108813be`, p1-w5 `399cc29b` stale, p1-w6 `d956237a` stale): w1 `734ecb32`, w2 `95b43932`, w3 `7ce32518`, w4 `2710f503`, w5 `e20953f5` (stale), w6 `6fb7dc4e` — all six values identical to the 13:58 round. Zero crons-array drift since 13:58; no re-diff of tracked rows needed given the hash match.

**Open question, not logged as a finding (flagging only — evidence is mixed, not fixing):** while sweeping fresh ground (leads/quotes/referrals/documents/invoices surfaces — all found already thoroughly audited by prior work this session, no new bug there), noticed `POST /api/prospects` and `POST /api/admin/requests` both insert ~14 fields into `partner_requests` (`billing_address/city/state/zip`, `growth_goal`, `automation_comfort`, `lead_gen_spend`, `pain_point`, `timeline`, `wants_automation`, `wants_growth`, `comparing_prices`, `fit_score`, `fit_bucket`) that appear in zero tracked migrations for that table — same static-analysis signal as the campaign_recipients finding (142b7bdc) that turned out to be a real gap. BUT unlike that case, two things argue against this being broken: (1) `partner_requests`' own base schema is *also* only documented via an inline SQL comment in `requests/route.ts`, never a tracked `CREATE TABLE` — this table's whole schema has evidently drifted via untracked hand-run ALTERs as a matter of course, not just these 14 columns; (2) `POST /api/admin/requests` (admin manual lead entry) does NOT catch/swallow the insert error — a real missing-column bug there would 500 loudly on every manual lead add, which would very likely have already been noticed and fixed given `admin/sales/LeadsPanel.tsx` and `ContactsPanel.tsx` both actively read `fit_score`/`fit_bucket` back out. No live Supabase env in this worktree to check `information_schema.columns` directly. Not preparing a migration for this — doing so without confirming the columns are actually absent would be guessing, and the loud-500 admin path is a real signal against it. Worth a 30-second live check (`\d partner_requests`) whenever someone has DB access; if the columns are confirmed present this is a non-issue and this paragraph can be deleted.
