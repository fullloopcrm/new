# Broad-hunt sweep — 23:30 order — W4, 2026-07-15/16

File-only, no push/deploy/DB. No code changes this pass — genuine review,
no new gap found.

## Approach

Prior W4 sweeps have already covered nearly every route directory under
`platform/src/app/api` (rbac gating, rate limits, IDOR, mass assignment,
HTML injection, timing-safe compares, npm audit). To find fresh surface I
cross-referenced `deploy-prep/*.md` mention counts against the route
directory listing and picked the lowest-coverage directories, then read
each route file directly rather than re-running past search patterns.

## Read and verified clean this pass

- `service-area/route.ts`, `service-types/route.ts` — tenant-scoped via
  `getTenantForRequest`/`requirePermission('settings.edit')`, no gaps.
- `setup-checklist/route.ts` — per-tenant onboarding progress, no
  permission gate needed (any authenticated tenant member toggling their
  own tenant's checklist is expected behavior, not privilege-sensitive).
- `sales-applications/route.ts` — public POST rate-limited by IP (3/10min),
  admin GET/PUT/DELETE gated by `team.view` + tenant-scoped, tenant
  resolved by slug lookup (not spoofable to cross-tenant write).
- `quote-templates/route.ts`, `recurring-expenses/route.ts` +
  `recurring-expenses/[id]/route.ts` — permission-gated
  (`sales.*`/`finance.*`), tenant-scoped, PATCH uses an explicit field
  allow-list (no mass-assignment).
- `migrate-cleaner-notifications/route.ts`, `migrate-sms/route.ts` —
  inert legacy compatibility shims (nycmaid one-shot migrations, obsolete
  on fullloop), gated, no-op bodies.
- `unsubscribe/route.ts` — signed-token-only (`verifyUnsubscribeToken`),
  no ID-guessing surface.
- `track/route.ts` — public analytics beacon, already IP-rate-limited
  (240/min), escapes HTML in the lead-notification email, has its own
  per-tenant email-bomb cap independent of the spoofable session_id.
- `indexnow/route.ts` — CRON_SECRET path uses `safeEqual`; admin path via
  `getTenantForRequest`; IndexNow itself validates urlList against the
  submitted host, so cross-tenant/cross-domain submission isn't useful
  even without an extra check.
- `docs/route.ts`, `changelog/route.ts`, `changelog/[id]/route.ts` — read
  only, gated (`settings.view`/auth check), no user-controlled query
  params reach the DB unsafely.
- `cpa/[token]/year-end-zip/route.ts` + `finance/cpa-tokens/route.ts` —
  token is `randomBytes(24)` base64url (192 bits), checked against
  `expires_at`/`revoked_at`; token creation already has the entity_id
  cross-tenant-FK check (comment references the P4-P6 leak-register fix
  class). Brute force is infeasible at this entropy; no rate limit needed.
- `client/booking/[id]/route.ts`, `client/bookings/route.ts`,
  `client/notes/route.ts`, `portal/notes/route.ts`, `portal/config/route.ts`
  — all go through `protectClientAPI`/`verifyPortalToken` before touching
  tenant data; the `client/bookings` duplicate-client phone/email matching
  (legacy multi-record clients) only runs after the caller's own phone has
  already passed the `>= 10 digits` floor, so it can't be tricked into a
  broad match via a short/garbage number.
- `team-portal/update-phone/route.ts`, `team-portal/checkin/route.ts` —
  signed short-lived token (`safeEqual`-verified) or portal JWT
  (`verifyToken`), booking ownership double-checked
  (`booking.team_member_id !== auth.id`).
- `feedback/route.ts` — anonymous POST already IP-rate-limited (5/hr),
  admin GET/PATCH gated by `requireAdmin()`.
- `auth/login/route.ts` — DB-backed fail-closed rate limit (5/5min),
  `safeEqual` PIN compare, empty-secret bypass already guarded, security
  notifications on lockout/near-lockout.

All of the above were already hardened by earlier sweeps or built correctly
from the start; none needed a new fix this pass.

## Verification

Read-only pass — no edits made, so no `tsc`/test run needed. `git status`
confirms no tracked files under `platform/src` were touched by this sweep.
