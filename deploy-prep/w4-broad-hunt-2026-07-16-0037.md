# Broad-hunt sweep — 00:37 order — W4, 2026-07-16

File-only, no push/deploy/DB.

## Carryover committed first

The prior session's team/cleaners `photo_url`/`avatar_url` scheme-validation
fix (reported at 00:35, verified tsc-clean) was written but never committed.
Committed now: `a8975fab` (code) + `96411204` (docs).

## Fresh angles checked this pass — all clean, no new exploitable gap found

- **`dangerouslySetInnerHTML`** — zero usages repo-wide. Clean.
- **`localStorage`/`sessionStorage` token storage** — audited every site
  storing auth-adjacent data. The global team portal (`src/app/team/*`,
  `/api/team-portal/*`) uses a proper HMAC-SHA256-signed, timing-safe-verified,
  scope-isolated, expiring bearer token (`src/app/api/team-portal/auth/token.ts`)
  — already has dedicated isolation/enumeration test files, well hardened.
  The client portal (`/api/client/*`) uses an httpOnly, `sameSite=strict`,
  tenant-bound HMAC session cookie (`lib/client-auth.ts`); `protectClientAPI()`
  correctly rejects any caller-supplied `client_id` that doesn't match the
  session's own id — no IDOR.
- **One raw-ID-trust pattern found, but not exploitable**: the legacy
  `wash-and-fold-hoboken`/`wash-and-fold-nyc` per-tenant clones (flagged as
  known architecture debt in `platform/CLAUDE.md`, "do NOT extend") have a
  team dashboard that stores a plain `cleaner_id` in `localStorage` (no
  signature) and sends it as a trusted query param to `/api/team/jobs`,
  `/api/team/availability`, etc. Checked whether those endpoints exist and
  trust the param blindly — they don't exist at all (`/api/team/login`,
  `/api/team/jobs` etc. have no route handlers anywhere in `src/app/api`;
  the real global routes are under `/api/team-portal/*`). This entire clone
  dashboard 404s on every API call — dead/non-functional code, not a live
  vulnerability. Not fixed (nothing to fix; matches the already-documented
  cutover-pending debt, out of scope for a security pass).
- **GET-handler routes that mutate data** (CSRF-via-GET / unsafe-method
  check) — swept all `route.ts` files with a GET-only handler containing
  `.update()/.insert()/.delete()/.upsert()`. ~30 hits, all but one are Vercel
  cron routes (GET is the platform-mandated convention for cron, gated by
  already-audited timing-safe `CRON_SECRET` bearer check — not
  cookie-authenticated, so not CSRF-reachable). The one admin-session route
  (`admin/comhub/contacts/[id]/context`) only persists a deterministic
  phone/email auto-match already computed from tenant-scoped data the admin
  can already read/write via the UI, and admin cookies are `sameSite`-scoped
  (confirmed by the 23:xx pass) — negligible impact, not treated as a new
  finding.
- **New scripts added this session** (`safe-channel-append.py`,
  `nycmaid-data-parity-check.mjs`, `append-atomicity-*.sh`) — spot-checked
  for command injection / secret handling. `safe-channel-append.py` uses
  `argv` directly with no shell invocation, explicitly labeled "proposal
  only, not wired into any live driver." Clean, non-issue.

No new exploitable gap found this pass. Idle, awaiting next order.
